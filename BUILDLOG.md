# Build Log

Reverse-chronological record of notable changes to the repo.

## Entry conventions

Three entry types, appended in reverse-chronological order:

- `<sha> — #<n> — <summary>` — a committed issue; counted toward the periodic audit threshold.
- `[AUDIT] <sha> — <summary>` — full-system architectural audit, run on every 5th counted entry; excluded from the count.
- `[HALT] #<n> — <reason>` — segment halted at the impasse stop condition; the work is not committed.

The run-time Live-log ledger (per-increment `[HH:MM] elapsed=…` lines) lives in `docs/RESUME-STATE.md`.

## 2026-07-10

- 707f4fc — #315 — fix(backup): include `admin.hash` in snapshots so a restore keeps the hosts logged in. A live restore drill found `scripts/backup.js` snapshotted `app.db` + `uploads/` + `thumbs/` only — `admin.hash` was never copied — so a restore that followed the README recovered guest sign-in, leaderboard, feed, and photos but left `POST /admin/login` hitting the handled missing-hash HTTP 500, locking the couple/planners out of moderation, awarding, and export. `backupData` now takes an optional `adminHashPath` and copies the hash into the snapshot when present, via a new `copyFileIfPresent` helper mirroring `copyPhotoDirIfPresent`'s if-exists tolerance (a never-set admin password is not a backup failure); `config.ADMIN_HASH_PATH` is passed through the CLI block so the path keeps its own env override, exactly like the other three. README: `admin.hash` added to the backup-contents list and the "Restoring" copy steps (bash + PowerShell, with a skip note when the snapshot has none and a line on why it matters). Tests: present → byte-identical in the snapshot (AC1); absent → backup still succeeds; `restoreFromSnapshot` restores it and the restore test asserts byte-identical recovery at the exact path the login route reads (AC2) — a mutation check (removing the copy) fails both new assertions. Issue-review PASS (1 Opus); PR + design-philosophy review PASS (2 Opus, 0 defects), bound to tree `c4e12ba5`; rebased onto `origin/main` past #252/#285 before review (the README backup/restore region was untouched by that chrome/SEO work, so the reapply was conflict-free). Non-visual change (`scripts/`, `README.md`, `tests/`) — no visual-approval loop. Merge-on-green after one update-branch cycle against the concurrent wave (PR #374, merge d689188).
- 3f1446f — #309 — test(loadtest): split the Goal-A load-test gate's single error bucket into distinct `server5xx` and `networkFailures` counts, each attributed to a request path via a new `byPath` breakdown. Removes the `NETWORK_FAILURE_STATUS = 599` sentinel that folded client-side connection drops into the same `>=500` bucket as real server errors — the flaky/blind-gate root cause found by the Wave-1 post-merge review and independently by testing (#316, absorbed here). A network failure is now identified only by an explicit `networkFailure: true` flag with `status: null`; the classification rule lives in exactly one place (`summarize`). The pass bar keys off `server5xx === 0` — network failures are reported but never, by themselves, fail the run. Adds an exported `formatSummary()` emitting a per-path failure breakdown, and a dated `## Recorded baselines` section in `docs/loadtest.md` (carries #316 AC3). AC1–AC4 unit tests bound to the real `summarize`/`evaluate`/`formatSummary` exports; loadtest suite 27/27, full vitest suite green on the rebased base (713). Issue-review PASS (1 Opus); PR + design-philosophy review PASS (2 Opus, 0 defects), bound to tree `0e491f06`; rebased onto `origin/main` past #252/#285 (BUILDLOG-only collision; the three reviewed files were unchanged by the rebase). Non-visual change (`scripts/`, `tests/`, `docs/`) — no visual-approval loop.
- `00afe19` — #285 — feat(seo): block search-engine indexing site-wide. Three independent, individually-reversible layers keep the intentionally-public gallery/leaderboard/feed/photo/profile pages out of search now that the app runs on a stable public domain: `src/public/robots.txt` (disallow-all, served by the existing `express.static(PUBLIC_DIR)` mount at `/robots.txt`); one `X-Robots-Tag: noindex, nofollow` response-header middleware in `src/app.js` section 3, placed **ahead of** the static mounts so it also covers the photo files under `/uploads` and `/thumbs`, which carry no HTML meta tag; and `<meta name="robots" content="noindex, nofollow">` in the shared `head.ejs`, covering every rendered page including the 404/error/maintenance cards (they include `head` transitively via `message-card.ejs`). `tests/noindex.test.js` asserts all three ACs. #284's headers middleware had not landed, so this created the single headers middleware fresh. Issue-review PASS (0 defects); round-1 `reviewer-pr` + `reviewer-design-philosophy` both PASS on tree `eee1eda3`. Rebased onto current `main` after #252 (merged mid-build) rewrote `head.ejs` (Wedding Master title + self-hosted fonts); the only conflict was in `head.ejs` — resolved by keeping #252's content and inserting just the meta line — with a fresh `reviewer-pr` PASS on the rebased tree `6c2f015b`. Full suite green (708). `DESIGN.md` § Hosted deployment already recorded the exact three-layer posture (#290) — docs verified, no drift. Merge-on-green (PR #371, merge `0948865`).

## 2026-07-09

- `<sha>` — #245/#246/#252 — Wedding Master chrome, one owner-approved visual batch shipped together (shared `header.ejs`/`theme.css`/`guest.js` made three separate PRs unsafe). **#252 (masthead + menu):** guest masthead — script "Wedding Master" wordmark (self-hosted Great Vibes `@font-face`, `font-display: swap`; every `fonts.googleapis.com` request removed, Cormorant/EB Garamond self-hosted too per AC3) over a letter-spaced `♥ LILLY & AXEL ♥` line, two nav rows with `req.path`-derived `aria-current`, opaque non-sticky header; profile bottom menu cut to three rows (Share a memory → `/memories/new`, How to play, Report a bug muted) after the owner cut the redundant Tasks/Gallery/Leaderboard rows live in the visual-approval loop; footer/titles rebranded. `res.locals.currentPath` single-homed in `session.js`. **#246 (how-to-play):** `GET /how-to-play` guest-gated rules card with a live `is_active` task count (never hard-coded), first-undone-task CTA, and `POST /onboard` redirecting to `/how-to-play?first=1` (Skip link only then); rule order + copy owner-revised in the loop (no em dashes). **#245 (report-a-bug):** guest `GET`/`POST /bug-report` (requireGuest **403** gate — AC corrected from the filed 401 to the app-wide convention), 1000-char body cap, referer/user-agent capture, success flash; admin `GET /admin/bugs` queue + `POST` resolve toggle; new `bug_reports` table (`ON DELETE CASCADE`). Visual-approval loop reached explicit owner approval live in Chrome across several copy/size/order revisions. Review: round-1 batch panel — `reviewer-pr` + `reviewer-design-philosophy` + `reviewer-security` (advisory) all PASS on tree `842b0235`; PR + security converged on an unbounded `/bug-report` path (minor) → misleading comment corrected, hardening tracked as backlog #369; round-2 fresh `reviewer-pr` PASS on the shipped tree `d1dd28f`. Doc-currency corrected `docs/architecture.md` (seven→eight tables, route lists) + `README.md`. Full suite green (705). Backlog filed: #368 (Share-a-memory as a pinned task card), #369 (bug-report rate limit).
- `577abaa` — #357 — fetch-fresh worktrees, overlap-aware freshness, and wave alignment. Prompted by a real incident the same day: a `/build` session passed every gate (5-round issue review, full system-level PR review, bias gate) on a worktree cut from local `main` 76 commits behind `origin/main` — a sibling issue (#248) had already rewritten `src/views/feed.ejs`, a file the session also touched, and the review certified work against an abandoned base; a human clicking around the running app was the only thing that caught it. `tools/new-agent-worktree.ps1` now fetches `origin/main` first and bases a **new** branch on it explicitly (never local HEAD), fails loud and creates nothing on a fetch failure, and leaves an **existing** branch un-rebased on resume. `tools/check-freshness.ps1` gains an overlap-aware signal: given a file list (an issue's or wave's `Touches`, or the branch's own diff when none is passed), any non-carved-out overlap with what `origin/main` changed since the branch forked is a hard resync trigger regardless of commit count; `MAX_DRIFT_COMMITS` (default 10, single-homed here, not in `config.js`) escalates the message on sheer count alone; the carve-out list is exactly `BUILDLOG.md` in this slice. `.claude/commands/build.md` step 0 and `agents/orchestrator.md`'s isolation rule now run the freshness check against the freshly-cut worktree and state that the primary checkout's own behind-count is bypassed, never a blocker. New `tools/check-wave-alignment.ps1` flags intra-wave `Touches` collisions before a wave launches (dot-sources `check-freshness.ps1`'s carve-out list rather than keeping a second copy — a drift guard proves the two tools agree). New `.claude/commands/realign.md` is the between-waves mechanical complement to `post-wave-review.md`'s post-merge judgment: safe fast-forward of a clean, non-diverged local `main`, then next-batch overlap reporting via the same two tools. New `tests/check-freshness.test.js` + `tests/check-wave-alignment.test.js` (behavioral, real scratch git repos) cover behind-count reporting, the threshold boundary, overlap detection including the carve-out, and the carve-out-agreement drift guard.
- 837c461 — #247 — feat(photos): share-a-memory — task-free batch uploads to the shared gallery. Guests upload photos that match no task straight to the gallery (up to 10/batch) via `GET /memories/new` + `POST /memories` (Goal D). Schema: `submissions.task_id` made nullable (a memory = `task_id IS NULL`) by a guarded table-rebuild migration (`ensureTaskIdNullable`, mirroring the `ensureBadgeTypeCheckWidened` recipe); `UNIQUE(guest_id, task_id)` is kept — SQLite's NULL-distinct semantics let a guest hold any number of memories. Scoring/badges: memories earn no automatic base point and are excluded from `MOSTPHOTOS`, while keeping per-photo admin-bonus eligibility — the base point stays a single authority in `scoring.js` (`photoPoints` gained a `hasTask` flag; `stmtCompletedCount` and the leaderboard base-count filter `task_id IS NOT NULL` while `SUM(photo_bonus)` still spans memories); `COMPLETIONIST` is task-keyed and unaffected. Display: gallery `Memories` section, feed "a shared memory" card, My Photos, admin `Memory` label; taken-down memories stay hidden everywhere (LEFT JOIN conversions preserve `taken_down = 0`). Abuse guardrails: a per-guest in-process rate limit + a global disk-space guard on the memory route (`src/services/rate-limit.js`, `config.js` `MEMORY_RATE_MAX`/`MEMORY_RATE_WINDOW_MS`/`MIN_FREE_DISK_BYTES`) — neither caps lifetime memory count (Goal D); they bound rate and protect the host disk (Goal A). Entry points were owner-revised in the visual review: a `Share a memory` button in the profile nav, a persistent task-board button that promotes to a highlighted "you've finished every task" card once all tasks are done, and the task-success prompt; the originally-planned gallery-header button was dropped to keep the gallery clean. Issue-review passed a 5-round adversarial loop (caught the badge-flooding, scoring base-count, and My Photos wiring gaps). The worktree was found 76 commits behind `origin/main` late in the build (see #357); the branch was rebased onto current main and the feed-card slice reworked against feed-card v2 (#248), resolving a duplicate `setFlash` from that refactor. Final PR review on the rebased tree passed the system-level bar (edits `DESIGN.md`/`config.js`): two independent Opus `reviewer-pr` + `reviewer-design-philosophy` (0 defects) + `reviewer-security` (no escalation), bias-gate clean, all bound to tree `656b7d17`. Full suite green (652). CodeQL `js/incomplete-sanitization` #56 on `tests/memories.test.js` dismissed used-in-tests (a hardcoded single-apostrophe literal; non-exploitable, per the #58 triage). Follow-ups filed during review: #357 (worktree staleness → wave-alignment), #363 (badge art redesign, backlog), #364 (batch-upload affordance). Merge-on-green (PR #365, merge f905855).

## 2026-07-08

- 18a9059 — #291 — docs: rewrite README + architecture for web hosting; stamp historical docs. The human-facing docs still taught the laptop + Cloudflare-quick-tunnel model; this rewrote current-operation docs to the hosted model (per DESIGN.md's "Hosted deployment" ADR, #290/#282) and stamped the historical ones. `README.md`: intro, "Going live", and Backups rewritten for a rented Linux host with a persistent disk, HTTPS at the host's reverse proxy, and scheduled off-host backups (`BACKUP_RETENTION_COUNT`), with the full runbook forward-linked to `docs/deploy.md` (created later by #286/#287); Quickstart keeps Windows/PowerShell as the dev env and notes Linux for prod; the "Working in a worktree" subsection is unchanged; `cloudflared`/`trycloudflare`/`quick tunnel` now absent from README. `docs/architecture.md`: the request-path diagram's TOP is redrawn — tunnel/Windows-laptop nodes → an `HTTPS reverse proxy (host)` node into an `app server` subgraph — while the middleware/router/service internals (incl. the #307/#318 route + service annotations) are left untouched (#291 AC4). `docs/loadtest.md` + `docs/test-plan.md` reframed as living pre-deploy checks (loadtest Goal A quote matches the #289 north-star wording). `docs/product-brief.md`, `PLAN/00-README.md`, `PLAN/01-setup.md`, `PLAN/03-auth-and-links.md`, `docs/reviews/2026-06-28-adversarial-review.md`: a `hosting model changed 2026-07` blockquote stamped after each H1, pointing to DESIGN.md, bodies unchanged. `docs/deploy.md`/`BACKUP_RETENTION_COUNT` are forward references by design. Depends on #289/#290. Routine tree: issue-review reviewer-issue PASS; PR review two independent Opus (reviewer-pr + reviewer-documentation), both PASS on tree `cff09a25` (nits only, one plan-authorized); doc-only so the design-philosophy gate is N/A. Residual tunnel refs in unstamped `PLAN/08-admin.md`/`PLAN/09-export.md`/`docs/security/codeql-triage-2026-06.md` are out of this issue's Touches (PLAN/ is umbrella-stamped via its `00-README` index; the codeql file is a dated forensic record) — filed as a follow-up. Merge-on-green.
- f236b56 — #282 — feat(server): trust proxy, /healthz probe, graceful shutdown. Makes the app behave under a hosting platform's reverse proxy and supervisor. `config.js` gains `TRUST_PROXY` (parse rule in `resolveTrustProxy`: unset/empty → `false`; the string `'true'` → `1` — trust exactly one hop, never Express boolean `true`, which would trust a spoofable `X-Forwarded-For` chain; a positive integer → that hop count; `'0'`/anything else → `false`) plus a `config.trustProxy` alias; `src/app.js` sets `trust proxy` only when not `false` (unset leaves Express's own default). New `GET /healthz` runs `SELECT 1` on the live SQLite handle → `200 {"ok":true}` or `503 {"ok":false}` if it throws, mounted ahead of maintenance mode (stays up in a maintenance window) and ahead of `attachGuest`/the routers (no session cost; never rate-limited by placement once #283 lands). New `src/utils/shutdown.js` — `installShutdownHandlers(server, { db, exit, timeoutMs, log })` returns an idempotent `shutdown(signal)` that drains (`server.close`), closes the DB, exits 0, with an `unref`'d force-exit backstop (10s); `SIGTERM`/`SIGINT` handlers register only inside the `require.main === module` guard so importing the app under test attaches no real signal listeners. Startup log advertises `config.BASE_URL` when set and carries no `cloudflare` remnant (folds in the owner-approved 2026-07-08 retirement of #288). Doc loop closed: DESIGN.md § Hosted deployment gains a "Process lifecycle (#282)" paragraph recording the `/healthz` + shutdown behaviors the #290 ADR did not yet cover; `docs/deploy.md` intentionally not created (that runbook is #286's scope). New `tests/hosting-lifecycle.test.js` covers AC1–AC6 by value (the AC3 env-reload evicts `../config` + `../src/app` from `require.cache` — `vi.resetModules()` alone does not re-execute CJS here). System-level tree (touches `DESIGN.md`): issue-review reviewer-issue + reviewer-architecture PASS over 2 rounds (round 1 fixed a false provenance claim — line 3 had attributed `/healthz`+shutdown to the ADR, which records only `TRUST_PROXY` — and added AC5-placement + AC6 BASE_URL/cloudflare coverage the plan's scope amendment lacked); PR review two independent Opus reviewer-pr + reviewer-security PASS plus reviewer-design-philosophy PASS (0 defects) on tree `33d18cf`, bias-gate clean before each fan-out. Full suite green (632). CodeQL `js/missing-rate-limiting` #55 on `/healthz` dismissed won't-fix on the ground recorded in DESIGN.md § Hosted deployment ("Process lifecycle (#282)": the probe is mounted ahead of the routers, so it is never rate-limited by placement) and confirmed by reviewer-security — an unauthenticated, `SELECT 1`-cheap liveness probe must answer the platform's health check, so throttling it would defeat its purpose. (This is distinct from `docs/security/codeql-triage-2026-06.md`, whose general `js/missing-rate-limiting` stance is superseded by #283 adding a limiter to the guest/admin routes; /healthz's exemption is a placement decision, not that doc's disposition.) Merge-on-green after several update-branch cycles against the concurrent wave (PR #348).
- 40e7fc7 — #302 — feat(commands): add the `/post-wave-review <N>` slash command (`.claude/commands/post-wave-review.md`) — the post-wave consultant quality-gate prompt, installed so the wave-integration review launches with one command instead of a pasted transcript; the wave number is parameterized via `$ARGUMENTS` (`wave-$ARGUMENTS` renders `wave-2` for `/post-wave-review 2`). System-level tree (`.claude/`): two independent Opus reviewers (`reviewer-pr` + `reviewer-documentation`), both PASS on tree `be308f7`, bias-gate clean each fan-out; issue-review PASS (Opus `reviewer-issue`), re-reviewed after the amendment below. The authoritative template — transcribed from an older chat — still saluted "Fable", the model retired 2026-07-06 (#274); `reviewer-documentation` FAILed it as a stale consumer (a command authored 2026-07-08 must not address a retired model). Fixed by scrubbing the salutation to model-neutral (`We'd value your judgment on the state of this app`) and amending issue #302's AC1 to mandate the neutral string plus assert `Fable` is absent from the file, then re-running issue-review + both code reviewers fresh → all PASS. A prettier `--check` on the new `.md` forced one blank line before the `HOUSE RULES` list (proseWrap preserve, so no AC substring moved); the reviewers re-ran once more on the formatted tree `be308f7`. BUILDLOG kept out of the change PR to dodge the mid-wave conflict, backfilled here. Merge-on-green (PR #346, merge 48ff6a9).
- 5b438a0 — #313 — fix(tests): isolate `npm test` from the live `data/app.db`. `src/db.js:15` opens `config.DB_PATH` and runs schema DDL + the guarded migrations at REQUIRE time, so any test whose top-level requires reached it bound the process to the real `data/` dir — the wave-1 review reproduced `npm test` advancing `app.db-shm`'s mtime and migrating the live `guests` table (the #239 identity columns) out-of-band. Audit found exactly one offender: `tests/smoke.test.js`, whose two top-level requires (`../config`, `../src/services/scoring`→`../db`) ran before any `DATA_DIR` override (`identity.js`, top-level-required by `guest-identity.test.js`, is pure; every other db-reaching require sits inside a `beforeAll`/`it` hook). Fix points `process.env.DATA_DIR` at an `mkdtempSync` temp dir before those requires (loadApp()'s idiom). New `tests/helpers/global-setup.js` (wired via `vitest.config.mjs` `globalSetup`) snapshots the mtimes of `data/app.db{,-wal,-shm}` before the suite and fails the run if any changed — enforcing AC1 on every future CI/local run; it no-ops cleanly when the files are absent (fresh checkout / CI, AC3). A vitest 4.1.9 gotcha confirmed by spike: a thrown teardown error is swallowed, so the guard sets `process.exitCode = 1` directly (verified: an all-pass suite that touches the watched db exits 1). Routine tier: reviewer-pr + reviewer-design-philosophy both PASS (round 1 on tree `4aaa1ad`; a version-string comment nit fixed → both re-run fresh and PASS on committed tree `295ba83`), bias-gate clean before fan-out. Full suite green in the worktree (546) and on CI (566 on the merged tree), no `data/` created. Merge-on-green (PR #347, merge e774270).
- ae24a3e — #318 — docs: fix data-model/route/feature-surface + badge-threshold owner drift. Sibling of #307 (which landed the architecture.md schema/route/service surface); this cleared the remaining drift across the live doc surface. `docs/architecture.md`: the `admin.js` node annotation now names `/admin/comments` (moderation) and `/admin/badges` (custom-badge creation) — the #307-landed ER diagram / community node / services subgraph left intact. `README.md`: "What it does" gains a Feed/likes/comments bullet + admin per-photo-bonus/comment-moderation; "How it is used" names `/feed` and `/admin/comments`; the `config.js` "Where things live" line no longer claims to own badge thresholds. `DESIGN.md`: the badge-threshold ADR header **and** body corrected — thresholds live in `src/services/scoring.js` (config.js has no `BADGE_THRESHOLDS`; the copy was deleted in #118), "single source, no second copy" kept and header/body made consistent. `CONTEXT.md:24`: Badge glossary entry attributes thresholds to `scoring.js`, matching its own already-correct scoring-authority line 52. `CLAUDE.md:78`: the "Config is central" convention no longer lists badge thresholds among what `config.js` owns. `PLAN.md` + `docs/product-brief.md`: `PLAN/` relabeled historical/superseded, not authoritative. Historical records (`docs/reviews/*`, `PLAN/**` internals) left unchanged. Issue scope was narrowed post-#307 and expanded during issue-review to cover CONTEXT.md/CLAUDE.md. System-level tree (`DESIGN.md` + `CLAUDE.md`): issue-review reviewer-issue + reviewer-architecture — architecture FAIL rounds 2–3 (missing `CONTEXT.md`; then the `DESIGN.md` ADR **header** left ungated by AC3) → both folded into ACs → PASS; PR review two independent Opus (reviewer-pr + reviewer-documentation), both PASS on tree `0153eed6`, bias-gate clean (auditor FAILed the first briefing for pre-framing the ACs as string-checks + naming source files → corrected before fan-out); doc-only so the design-philosophy gate is N/A. Merge-on-green.
- 07304a9 — #248 — feat(feed): feed card v2. Reordered the feed photo card top-to-bottom (guest name above the photo → photo → caption → one action row: a rounder inline-SVG heart + like count, a comment-bubble + count, points right-justified → up to 4 one-line-clamped comment rows → `See all N comments` → a muted task line), retiring the pointy `&hearts;` text glyph and the always-open inline comment form. The full thread + composer moved into a per-card native `<dialog>` opened with `showModal()` — the top-layer render escapes the card's `content-visibility` paint containment that had pinned a round-1 `position: fixed` bottom-sheet off-screen (measured live: panel at y=4005 in a 667px viewport, not guessed) — centered, background inert (one dialog at a time), body scroll-locked, sage `::backdrop`, Instagram-style auto-growing `<textarea>` + a plain-text `Post` control; comments post in place via `fetch` (`Accept: application/json` → `{comment, commentCount}`) with the existing redirect kept as the no-JS fallback. The mobile tap-highlight box is suppressed on the card controls with a `:focus-visible` ring kept for keyboard nav. Comment visibility (`taken_down = 0`) is centralized in one `COMMENT_VISIBLE_WHERE` predicate composed by both the rendered thread and the badge/See-all count, and `refreshCardPreview` takes an explicit submission id from the route contract rather than reverse-parsing the `photo-<id>` DOM id (both are round-2 design-philosophy fixes). Owner approved the rendered visuals at three phone form factors (pre-merge visual-approval loop, which surfaced and cleared the real round-1 off-screen-dialog bug and a stale-CSS-cache false alarm). Adversarial review: `reviewer-pr` PASS (all 9 ACs traced input→output) + `reviewer-design-philosophy` PASS on tree `e38cb315` over 2 rounds (round-1 design-philosophy FAIL on two `major` information-leakage findings → fixed, re-reviewed with fresh instances). CodeQL `js/missing-rate-limiting` on the `requireGuest`-gated comment POST re-surfaced by the diff (touched-line reattribution, same phenomenon as PR #138) → alert #45 dismissed won't-fix per `docs/security/codeql-triage-2026-06.md` (auth-gated + bounded, Cloudflare-fronted; a mis-set `trust proxy` limiter would sideline guests on venue Wi-Fi — Goal A). 20 new tests (`tests/feed-card.test.js`), full suite 566 green. Follow-ups filed and issue-reviewed: #338 (guests delete their own comments — ready, `Depends on #248`) and #340 (guests edit their own comments — backlog, graduates after #338). Merge-on-green (PR #341, merge 9a0a81c).
- 4b0f06e — #304 — docs(deps): the sharp 0.35.2 `ERR_DLOPEN_FAILED` / Application-Control block that cost #239, #254, and #192 a junction workaround was a Smart App Control **reputation-lag**, not a signing gap — and it has cleared. Re-tested on the host under SAC Enforce (`HKLM:\…\CI\Policy\VerifiedAndReputablePolicyState = 1`): a fresh `npm ci` loads sharp 0.35.2 (`node -e "require('sharp')"` exit 0) and `npm test` is green (69 files / 546 tests), with the `sharp-win32-x64-0.35.2.node` binary still `NotSigned` and carrying no mark-of-the-web — the exact conditions the block was pinned on — yet it loads (a genuine npm install, not a junction). SAC blocks a new/unknown unsigned binary by cloud reputation until its hash accrues one, then allows it. Decision: keep 0.35.2, no pin-back (a pin-back downgrades a wedding-critical, security-relevant image library to fix a non-reproducing failure that Dependabot would re-open). `DESIGN.md` records the finding, the keep-0.35.2 decision, and PR #14's tracked-decision gap (triaged `review`/"HELD for a tested decision", merged the next day with no recorded on-host smoke test — the gap that armed the landmine). `CLAUDE.md` § Dependency updates now requires an on-host `npm ci` + `require` smoke test before merging a `review`-tier bump to a wedding-critical dep that ships a prebuilt native binary (`sharp`, `better-sqlite3`), since CI (Linux) cannot catch the Windows-only SAC block on a new binary hash. System-level tree (`DESIGN.md` + `CLAUDE.md`): issue-review reviewer-issue + reviewer-architecture PASS; PR review two independent Opus reviewer-pr + reviewer-design-philosophy PASS on tree `51bf661d` over 2 rounds (round-1 design-philosophy FAIL on a dangling cross-reference that quoted a nonexistent CLAUDE.md rule name → fixed), bias-gate clean before each fan-out. No `package.json` / `package-lock.json` change; AC1/AC2 verified on the host. Follow-up nit (not blocking): the native-binary subset (`sharp`, `better-sqlite3`) is asserted in prose only, with no drift-guard test. Merge-on-green.
- 384eb14 — #322 — governance(cost): implementer duplicated-ownership self-check + researcher owner-surfacing before first review; graduates backlog #206 (its `Graduate after` — #201 merged 2026-07-05 — was met). Adds Build rule 7 `Duplicated-ownership self-check` to `agents/implementation-agent.md` (a required handoff field — answer `no`/`none`, or `yes` and name both locations + the single owner); names the recurring duplicated-ownership form of `information leakage` in `standards/design-philosophy.md` with the six real cases (#89/#87/#80/#78/#86/#88) and echoes the pattern name in `standards/design-philosophy-examples.md`; adds an `Existing owner of a named rule` findings section to `agents/researcher.md` plus a hand-owner-to-implementer step in `skills/research-prior-art.md`; and adds a bias-safe reconciliation to `agents/orchestrator.md` — the design-philosophy reviewer stays blind (the self-check answer is never placed in its briefing) and the orchestrator reconciles the reviewer's independent information-leakage findings against the self-check after the reviewer returns. Parent #206's literal AC2 (put the implementer's claim in the reviewer's briefing) was a planted-suspicion / implementation-leak bias violation, so it was graduated to the orchestrator-side reconciliation instead. System-level tree: issue review `reviewer-issue` + `reviewer-architecture` both PASS; PR review two independent Opus `reviewer-pr` + `reviewer-design-philosophy`, all PASS on tree `dc51475` (round 4 — round 3 caught a real major: the rule renumber had broken the `standards/edge-case-checklist.md:7` "build rule 6" cross-reference; fixed by keeping the edge rule at 6 and placing the self-check at 7), bias-gate recorded each round. Merge-on-green (PR #328, merge `8b543266`).
- 45bcdda — #321 — chore(tools): migrated `apply-branch-protection.ps1` from the closing-down `required_status_checks.contexts` field to GitHub's current `checks` array (each `{context, app_id: -1}`); `app_id: -1` ("any app") preserves the old name-only matching, and `strict: true` plus the five real check-run names are unchanged. Added an `-EmitPayload` switch that prints the PUT body as JSON and exits 0 **before any `gh`/network call** — an offline seam so CI regression-guards the payload shape with no GitHub credentials; slug resolution deferred to the network (PUT) path. New `tests/apply-branch-protection.test.js` asserts the five-check payload and the six-check `-RequireSmoke` payload through the seam. Graduated from backlog #146 (its `Graduate after: #114 / PR #143` condition was met — PR #143 merged 2026-07-02 — so #146 was opened as ready-tier #321 rather than implemented in place, then closed as superseded). System-level tree (`tools/`): issue-review reviewer-issue + reviewer-architecture both PASS (AC1 strengthened to assert `app_id = -1` per a reviewer minor); PR review two independent Opus reviewer-pr — round 1 both FAIL (the seam still ran `gh repo view` before emitting, which would redden the required `test` CI check with no `GH_TOKEN`), fixed by deferring slug resolution → round 2 both PASS on tree `4618de84` + reviewer-design-philosophy PASS, bias-gate clean. Merge-on-green (PR #329, merge e0c4e7d).
- 0a37d3c — #306 — docs(context): CONTEXT.md's "Vision terms — not built yet" listed Likes, Comments, and Per-photo points as having "no schema or route in the code today", but all three are shipped and live on `main` (verified: `likes`/`comments` tables at `src/db.js:82-97`; `POST /p/:submissionId/like` at `src/routes/community.js:364`; absolute-set `photo_bonus` at `src/routes/admin.js:654`, comment at `:629` confirms "ABSOLUTE SET"). Removed the three bullets and added a `## Language` entry recording their real surface; Prize/Slideshow/Favorites remain listed as genuinely unbuilt. Issue review PASS (Opus reviewer-issue, 2 minors, non-blocking). Doc-only, non-kernel tree: PR gate is the diff-scoped Opus `reviewer-pr` (matches the #289 docs precedent) — PASS, all 3 ACs verified against source, one immaterial nit; design-philosophy gate is N/A for doc-only. A whole-file `reviewer-documentation` pass was also run and FAILed on two **pre-existing** CONTEXT.md gaps unrelated to this diff (no user-story header; "Seams"/"Conventions" sections mixing architecture/behavioral content that lives nowhere else) — out of #306's `Touches: CONTEXT.md` scope and cross-file into kernel docs owned by concurrent wave sessions, so filed as follow-up issue #332 rather than expanded here. Merge-on-green (PR #325, merge ba38170).
- 09bc417 — #290 — docs(design): recorded the hosted deployment as the target and re-triaged the CodeQL rate-limiting dispositions, so the code issues (#282-#286) implement what the docs record. `DESIGN.md`: Constraints rewritten for one small Linux host with a persistent disk and TLS at a reverse proxy, ~100 guests over the public internet; `### Cloudflare quick tunnel for public access` replaced by `### Hosted deployment` (2026-07-07) recording reverse-proxy TLS, `TRUST_PROXY` (spec #282 implements), a stable load-bearing hostname the QR codes encode, and a deliberately non-indexable gallery (#285), with the tunnel model kept as a historical note; the export ADR points durability at `scheduled backups` (`scripts/backup.js`, scheduled per `docs/deploy.md`) instead of "the app's lifetime is the weekend"; the SQLite ADR names the `data/` `persistence boundary`; EJS and COOKIE_SECRET ADRs reworded host-agnostic; the execution-surface note re-derived from the hosting constraint; event mode noted unchanged by the move. `docs/security/codeql-triage-2026-06.md`: appends `## 2026-07 re-triage: hosted deployment` (2026-06 dispositions kept verbatim) superseding all three rate-limiting-adjacent dispositions (`js/missing-rate-limiting` main entry, the 2026-07-02 re-surfaced instance, the admin login lockout adequacy note) with a pointer to #283 and the changed exposure window (weeks public vs. hours tunneled). System-level tree (`DESIGN.md`): issue-review reviewer-issue + reviewer-architecture PASS (architecture flagged two minors — stale plan line-numbers and two removals not AC-gated; both carried into implementation as binding instructions and the tunnel/laptop phrases removed from the Constraints and EJS ADRs); PR review two independent Opus reviewer-pr, both PASS on tree `8d9e41b3`, bias-gate clean; doc-only so the design-philosophy gate is N/A. Blocks #282-#286. Merge-on-green (PR #326).
- b45cc26 — #145 — fix(tools): `check-gate.ps1` and `check-enforcement.ps1` printed remediation hints telling the operator to run `setup.ps1`, a file that does not exist — the real activation script is `tools/setup-hooks.ps1`, so following the hint could never activate the commit gate. Corrected all four references (`check-gate.ps1:14`; `check-enforcement.ps1:10,15,37`), preserving each line's existing invocation style (the `-ExecutionPolicy Bypass` flag stays on the line that already had it). AC1: the `check-gate.ps1` remediation line now prints `tools/setup-hooks.ps1`. AC2: `git grep "setup\.ps1"` over `tools/` and `.githooks/` returns zero matches. System-level tree (both files under `tools/`): bias-gate clean, two independent Opus `reviewer-pr` both PASS + `reviewer-design-philosophy` PASS on tree `4c178edd`, one round, 0 defects; issue-review PASS (Opus `reviewer-issue`). Follow-up filed: `.claude/hooks/session-greeting.ps1` still carries stale `setup.ps1` refs, outside this issue's `tools/`+`.githooks/` scope. Merge-on-green.
- cc9a53b — #181 — test(coverage): raised overall coverage 66.3→84.0% lines / 65.6→83.1% statements (596 tests) and ratcheted the `vitest.config.mjs` floors 62/65/53/62 → 80/84/71/80 (lines/functions/branches/statements) — the 80/80 standard #182 asked for is now enforced by the required CI `test` check (#182 itself was closed not-planned; #198's ratchet is the mechanism). Six new behavioral test files: admin guest management (bulk cap 500 + junk rejection, pin/unpin round-trip, bonus-points add/subtract/validation, delete survives a missing file), badge admin (metric-badge refusal per #80 AC5, custom badge create/duplicate/validation trio), task CRUD/reorder (add-to-top #258, neighbor swap, edge/bogus directions), moderation stale-post guards, export ZIP (per-guest folder naming incl. nameless fallback, missing-file skip, taken-down inclusion, workbook Guests/Submissions-with-likes+photo-bonus/Badges/Comments sheets per #192, `safeName` unit table, `.PNG`/no-extension fallback), and export pre-stream failure → 500. Four process-level one-shot scripts (`serve-resilient`, `ledger-push`, `loadtest`, `smoke`) excluded from the coverage denominator with stated reasons; `smoke` still runs as its own CI gate. Test selection authored by the orchestrator (`data/wip-issues/181-test-plan.md`); Sonnet implemented. Reviews: issue PASS (Opus); reviewer-pr PASS 0 defects + design-philosophy PASS 1 minor (independent Opus, bias-gated briefings — the auditor struck the orchestrator's test plan from the reviewer briefing as an answer key). Tree `6db4f721`, merge-on-green (PR #303, merge bd74f01). Follow-ups filed + issue-reviewed: #304 (sharp 0.35.2 win32 binary blocked by Smart App Control — third build bitten; #239/#254 junctioned, this build copied primary's trusted 0.33.5) and #305 (branch-coverage pass on guest-facing fallback arms — branches sit at 71.55%).
- 1c96a70 — #310 — governance(review): record wave grandfathering, owner-invoked wave review, and a Sonnet doc-currency step. `standards/adversarial-review-protocol.md` § "Wave governance (#310)" now records (a) **grandfathering** — a mid-wave governance/gate change governs from the next issue picked up onward, open sibling PRs merge under the bar in force when their implementation began, except a `severity:blocker` security gate change (immediate reach-back), worked example PR #295/#298; (b) the whole-of-wave review as `owner-invoked` (via `/post-wave-review`, #302) — never automatic, never a precondition for the next wave; (c) the `doc-currency` classification (implementer-side, adds no reviewer or dispatch-table row, `separate from any documentation-currency review`) plus the `docs-only` staging rule. `agents/orchestrator.md` gains the inline `model: sonnet` `doc-currency` step (sole owner of the trigger paths and the staged-before-verdict-binds mechanics, dispatched `concurrently` with PR review) and a distinct `## Wave boundary` nudge closing with `owner may run /post-wave-review`. `CLAUDE.md` § How work flows gets the owner-invoked pointer; `DESIGN.md` records the decision + rationale (doc-currency chosen over a flag-only reviewer per the #318 evidence). Orchestrator prior-art caught the pre-existing unwired `agents/reviewer-doc-currency.md` name collision → distinguished functionally in-scope, orphan retirement filed as #323 (backlog). Issue review: reviewer-issue + reviewer-architecture — architecture FAIL rounds 1–2 (docs-only vs exact-tree-binding contradiction; note-only disambiguation; AC2 wave-checklist placement) folded into testable ACs → PASS round 3. PR review (system-level): two independent Opus reviewer-pr PASS + design-philosophy PASS on tree `56fb59b` — design FAIL rounds 1–2 on information-leakage duplication (staging mechanics across orchestrator/protocol, then the trigger-path triple) resolved by single-owner + pointers; bias-gate clean each fan-out. Merge-on-green.
- 578a149 — #240 — feat(auth): shared entry link with self-serve signup at /join. Every guest uses the same link (QR poster / email / place card) instead of a per-guest token: `GET /join` renders a public form, `POST /join` creates a playing guest from name + email-or-phone contact + a self-chosen 4-digit PIN (+ optional avatar) and signs them in — signup is onboarding (`onboarded=1`). A contact that already exists routes to `/login` (#241, not yet built — #240 blocks it) rather than creating a second account; an invalid PIN / contact / empty name flashes and redirects back with no insert. `makeUniqueToken` moved to `src/services/identity.js` (shared with the admin guest-creation forms); the duplicated one-shot flash-cookie writer `setFlash` consolidated into `src/middleware/session.js` (its reader's home) and imported by `auth.js` + `guest.js`. Visual-approval loop: owner-approved at three phone form factors after a copy/UI pass — dropped the filler intro + per-field justifications, PIN helper down to `Any 4 digits, not your bank PIN.`, heading/CTA `Join the Fun` / `Start playing` (the "hunt" metaphor doesn't fit a task-based game), secondary outline optional-photo button, underlined re-entry link. Reviewed: issue PASS (reviewer-issue, recorded 2026-07-05); PR reviewer-pr + reviewer-design-philosophy (both Opus) — design-philosophy FAIL round 1 on the duplicated `setFlash` (information leakage) → fixed → both PASS on the final tree over 3 review rounds, bias-gate clean before each fan-out. 553/553 tests, lint 0 errors, format clean. Blocks #241/#244. Merge-on-green.
- 0b69a55 — #307 — docs(architecture): sync README + architecture.md to the real schema/route/service surface. The record still described a pre-wave shape; brought in line with `src/` at this wave: `docs/architecture.md` now says seven tables (adds `likes`/`comments` to the erDiagram + DDL summary), `guests` carries `contact`/`contact_type`/`pin`/`pinned`, `badges.type` is `auto|special|metric|transferable|custom`, and the UNIQUE-constraint list + the flowchart's `community.js`/`services/` nodes match the real routes and files; `README.md` extends the `community.js` route map (`/feed`, `GET /p/:submissionId`, `/p/:submissionId/like`, `/p/:submissionId/comments`), adds `submissions.js`/`feed.js`/`badges.js`/`identity.js` to the services list, and drops the fixed "seven badges" claim for the five badge types incl. admin-creatable `custom`; `docs/product-brief.md` badge section follows suit. Every claim verified against `src/db.js`, `src/routes/community.js`, `src/services/`, `scripts/badge-catalog.js` (the `photo_bonus` column and `idx_guests_contact` partial unique index are real, from migrations #89/#239). Orthogonal to #291's hosting rewrite on the same two files. Documentation-only tree (routine bar): issue-review reviewer-issue PASS (2 rounds — AC2's `/p/:submissionId` was subsumed by the `/like` route, tightened to method-prefixed `GET /p/:submissionId` + `/p/:submissionId/comments`); PR review reviewer-pr PASS (0 defects, all ACs verified against source) + reviewer-documentation PASS on each of the three files (minor/nit findings only, all pre-existing structure). Merge-on-green.
- 590954e — #289 — docs(north-star): reworded Scope and Goal A for the web-hosting migration. Goal A's "Holds up at peak" now reads `on venue wifi or their own connection` (was `on venue wifi.`); the Scope opening describes running this one event `on a small web host (live before, during, and shortly after the weekend)` with the single-event teardown (`then exported and taken down`) unchanged; the out-of-scope `a permanent hosting service` is disambiguated to `a multi-event hosting product` plus an in-scope parenthetical dated 2026-07-07; a change-log line records the hosting decision. `CLAUDE.md` verified (verify-only) — its North Star block names no network/teardown wording, so no edit. System-level tree (`docs/north-star.md`): issue-review reviewer-issue + reviewer-architecture (architecture FAIL rounds 1–2 on an AC-vs-plan placement contradiction, then the #291 DESIGN.md handoff being under-specified → both fixed → PASS round 3); PR review two independent Opus reviewer-pr, both PASS on tree `182dce13`, bias-gate clean; doc-only so the design-philosophy gate is N/A. DESIGN.md's laptop-only / no-paid-services deployment decisions (`:9`, `:10`, `:85-87`, `:155`) are the contradiction handed to #291 to reconcile; this change blocks #290/#291. Merge-on-green (PR #300, merge cf4bbca).
- a3a8fa7 — #196 — fix(admin): a deleted guest's avatar file is now removed from disk. `POST /admin/guests/:id/delete` hard-deleted each submission's photo + thumb but never touched `guests.avatar_path`, leaving a removed guest's portrait on disk and publicly servable at `/uploads/<file>` — the one file class the delete's file pass missed (worst for a data-removal request). Handler now reads `avatar_path` with the guest and calls `photos.deleteOriginalFile(avatar_path)` inside the same log-and-continue try/catch as the submission files (already no-ops on null, ignores ENOENT). New `tests/guest-delete-avatar.test.js` covers AC1-AC3 (avatar gone + `/uploads` 404, NULL-avatar guests unaffected, missing file does not abort the delete). Routine tier: independent Opus reviewer-pr + reviewer-design-philosophy, both PASS, one round (one comment-clarity nit fixed before commit). `npm test` 516/516, lint/format clean.
- 1a0649c — #254 — fix(upload): UPLOAD &AMP; label, uploading state, client-side downscale (PR #298). `task.ejs` rendered `<%= 'Upload &amp; complete' %>`, double-escaping to the literal `UPLOAD &AMP;`; now emits raw `Upload & complete` (EJS escapes once). Added an idempotent fetch-intercepting submit handler in `upload.js` that disables the button + swaps its label to `Uploading…` (a `dataset.uploadBound` guard is required because `task.ejs` loads the script directly AND via the footer `pageScript`, so it runs twice per page), plus pure DI-testable `downscaleImage`/`computeTargetSize`/`shouldDownscale` (canvas/image primitives injected so they test under vitest+jsdom, which has no canvas backend) that caps the long edge at 2000px / re-encodes JPEG q0.85 when a pick exceeds 2000px or 2.5MB and returns the original untouched otherwise; no-JS native POST preserved. Removed the duplicate `Choose a photo` form-label. Scope: task upload path only — the memory-batch form (`memory-new.ejs`) does not exist yet (deferred to the share-a-memory issue, which reuses `downscaleImage`); avatar uploads untouched. Reviewed: issue PASS (Opus reviewer-issue, 2 rounds — `# 254 —` header + Given/When/Then form); PR PASS (Opus reviewer-pr) + design-philosophy PASS (Opus), bias gate clean before each fan-out. 16/18 relevant tests pass locally; AC1/AC6 (supertest → `src/app.js` → `sharp`) blocked by this host's Application Control policy on the sharp `.node` binary (same block hits ~48 pre-existing files) and were confirmed by direct `task.ejs` render — CI on Linux is the authoritative gate. Merge-on-green.
- 72fcc55 — #239 — feat(auth): guest identity foundation — contact key + plaintext re-entry PIN. Lays the data foundation the shared-link signup (#240) and re-entry login (#241) build on: `src/db.js` gains nullable `contact`/`contact_type`/`pin` columns on `guests`, a guarded `ensureGuestIdentityColumns()` migration (same PRAGMA-guard pattern as `ensurePhotoBonusColumn`/`ensurePinnedColumn`; adds the columns on a pre-#239 DB, no-op thereafter), a partial unique index `idx_guests_contact` (`WHERE contact IS NOT NULL`, so one contact maps to one guest while contact-less legacy/seed rows coexist), and `getGuestByContact()`. New `src/services/identity.js`: `normalizeContact()` canonicalizes email (trim/lowercase) and phone (strip non-digits, drop leading US `1`, accept 10–15 digits) to `{type,value}` or `null`; `isValidPin()` is `/^\d{4}$/`. The PIN is stored plaintext by owner decision (threat model: guest mischief, not DB compromise — the hosts read a PIN back to a locked-out guest at the reception for Goal C), recorded in `DESIGN.md` and in the `identity.js` header so it is not later "helpfully" hashed. Issue review PASS (independent Opus). PR review: the first fan-out ran under a briefing whose environment note pre-exculpated a local sharp/jsdom load failure as "not a defect" — the bias-gate auditor FAILed it (builder-favoring); the note was corrected to "diagnose against the tree / report you could not run it," and both reviewers (reviewer-pr + design-philosophy, independent Opus) re-ran fresh on the corrected briefing → both PASS, 0 defects, bound to the staged tree. 527 tests green; lint 0 errors; format clean. Local test env: this worktree's freshly-installed sharp native binary is blocked by an OS Application Control policy and the primary checkout lacked jsdom, so `node_modules/@img` + `node_modules/sharp` are junctioned to the primary's allowed copies — a runner-only fix, no tree impact; CI (Linux) exercises the clean install.
- #192 — keepsake export now carries the social layer (Goal D). `src/services/export.js`: the summary workbook gains a fourth `Comments` sheet (Guest, Task, Photo Owner, Comment, Date, Hidden) covering ALL comments including admin-hidden ones — labeled `YES`/`no` in a Hidden column the same way the Submissions sheet labels a taken-down photo, so nothing a guest wrote is lost even after moderation; the Submissions sheet gains `Likes` (one grouped `COUNT(*) … GROUP BY submission_id` over `likes`, mapped by id — not a per-row query) and `Photo Bonus` (`submissions.photo_bonus`) columns so a guest's Guests-sheet total reconciles against the visible rows; every string cell still routes through the existing `neutralizeCell` CSV-injection guard. `streamExportZip` also drops each guest's avatar (when `avatar_path` is set and the file exists under `UPLOADS_DIR`) into their existing ZIP folder as `avatar<ext>`, mirroring the per-guest photo loop; existing sheets/columns and the archive layout are unchanged (additions only). New `tests/export-social.test.js` seeds a submission with `photo_bonus=2` + 3 likes, a visible/hidden/`=SUM(A1)` comment trio, and asserts AC1–AC3 by value. Reviewed: issue PASS (reviewer-issue); PR round-1 PASS (reviewer-pr + reviewer-design-philosophy, both Opus). Local test run blocked by this machine's WDAC/sharp `ERR_DLOPEN_FAILED` policy (pre-existing, unrelated) — lint/format green locally; suite gated on CI green before merge.
- 65b272c — #294 — docs(pipeline): reinstate a pre-merge visual-approval loop for visual changes, reversing the 2026-07-02 owner-merge-boundary retirement for visual changes only. The orchestrator, in the screenshot-capable `/build` main-loop session, boots the worktree's own app (not `.claude/launch.json`'s hardcoded primary-checkout path), captures three phone form factors (iPhone SE 375×667, iPhone 14 Pro Max 430×932, Samsung Galaxy S20 Ultra 412×915), and drives an owner approve/edit loop to explicit approval before the adversarial PR review; fail-closed (halt, never skip) if screenshot capability is absent. Non-visual changes unchanged (merge on review PASS + green CI). Reconciled `DESIGN.md`, `agents/orchestrator.md`, `.claude/commands/build.md`, `CLAUDE.md`, `standards/adversarial-review-protocol.md`, `WHAT-IT-CHECKS.md`, `docs/RESUME-STATE.md` so no doc both asserts and forbids the gate. System-level: reviewer-pr + reviewer-agent + reviewer-design-philosophy all PASS (one round); issue-review reviewer-issue + reviewer-architecture (architecture FAIL round 1 on wrong-tree launch and un-provisioned screenshot capability → fixed → PASS). Merge-on-green (PR #294).

## 2026-07-06

- ae92106 — #274 — docs(policy): Fable review patterns retired. The Fable model's access ended 2026-07-06; the #203/#207 patterns are marked Retired in `standards/adversarial-review-protocol.md` (decision text preserved below the notice), `DESIGN.md` § "Fable full self-certification exception" gains the dated addendum (persist-self-certification.ps1 + its test stay as the historical mechanism record; ValidateSet('fable') means no other actor can exercise it), and `CLAUDE.md` § Model policy is rewritten to past tense — every implementer now follows the unmodified protocol. System-level: one independent Opus reviewer + fresh-context self-cert, one round. Merge-on-green (PR #279, merge ae92106).
- 54e38e6 — #273 — docs(template): portable-vs-wedding-specific extraction map. New `docs/template-extraction.md`: the checked inventory seeding the owner's planned template repo — portable governance machinery (9 standards, 2 hooks, 20 tools/ gate scripts, 13 agents, 10 skills, CI backstops) vs wedding-specific content, plus parameterization points. Routine tier: fresh-context self-review, one round (FAIL: gate-script inventory missed check-issue-reviewed.ps1/persist-self-certification.ps1, wrong repo-slug claim, issue-guard mislabeled → fixed → confirm PASS). Merge-on-green (PR #278, merge 54e38e6).
- 5b37dcf — #268 #269 — feat(review): design-philosophy worked examples + severity calibration. New `standards/design-philosophy-examples.md` (Flag/Clean pair + `Not a finding:` over-flag guard per red flag, referenced from the charter and reviewer-design-philosophy); `agents/severity-adjudicator.md` gains a 7-row consequential/inconsequential calibration table; protocol § Calibration gains the finding-quality bar (blocker/major must state a concrete failure scenario; PASS cites per-item evidence) with a red-flag precedence carve-out. Both reviews FAILed round 1 on fabricated repo facts in the examples (badge JSON-column model, config.THUMB) — rebuilt on the real makeThumb/badge-registry designs; the self-review also caught a genuine cross-file contradiction between the new downgrade rule and the never-downgrade red-flag rule, fixed with the precedence clause. One independent Opus reviewer + fresh-context self-cert, one round each. Merge-on-green (PR #277, merge 5b37dcf).
- 0737fb7 — #267 #270 #271 — feat(standards): decision-heuristics charter + edge-case checklist + agent clarity. New `standards/decision-heuristics.md` (five explicit judgment procedures with worked examples: decide-from-goals, verify-before-claim, evidence-before-state-change, scope discipline, when-stuck ladder) referenced from orchestrator and implementation agents; new `standards/edge-case-checklist.md` (canonical input-type → edge table shared by implementer and reviewer-pr); reviewer-skill existence-check boundary, agent-standards model-tier tie-break, orchestrator worked ledger line + Qualifies/Does-not-qualify cascade examples. Independent review caught the examples' AC-form miss (blockquotes); the fresh-context self-review caught five fabricated repo facts (nonexistent rate limiter contradicting a recorded CodeQL won't-fix, phantom prize_id/deadline/re-entry-code features) — all regrounded on verified-real repo surfaces. One round each. Merge-on-green (PR #276, merge 0737fb7).
- 7f46579 — #266 #272 — fix(skills): PR ship-flow description + worked examples. `skills/github-write.md` frontmatter no longer claims direct-push/no-PRs (contradicted its own body and the orchestrator's ship flow); worked examples added to skill-writer (intent-not-words before/after), capture-system-defect (fix-now vs backlog scenarios with basis), issue-create (Haiku-bar plan-step pair), spawn-adversarial-review (end-to-end reviewer spawn-prompt skeleton — the repo's first). Independent review caught the example citing a phantom file/constant; fixed to the real src/services/photos.js ALLOWED_MIME_TO_EXT. One independent Opus reviewer + fresh-context self-cert, one round. Merge-on-green (PR #275, merge 7f46579).

## 2026-07-05

- d080947 — #194 + #251 (PR #264) — perf(feed)+fix(gallery): bounded thumbnail feed + 3-column gallery rework, shipped as one reviewed batch (both rework src/services/feed.js). #194: /feed now serves 40-photo keyset windows (FEED_PAGE_SIZE constant) of 400px thumbnails linking to /p/<id> for the original — server-side anchor resolution (?from=<id>) keeps every gallery/like/comment link landing on a page containing its photo, stale anchors degrade to the newest page; likes toggle in place via fetch (route answers {liked, likeCount} to Accept: application/json; form POST redirects to the bounded page as the no-JS fallback), viewer liked-state renders as aria-pressed + solid sage heart at a 44px target with a pop-on-toggle-only animation; /feed joined the loadtest read loop (reviewer ran it live: 100-concurrency 30s, 15598 requests, 0 errors, p95 343ms). #251: root-cause 522px-overflow fix at 375px (repeat(3, minmax(0,1fr)) + height-only contain-intrinsic-size: auto 124px; scrollWidth verified 375 exactly, live against the demo seed), like badges on tiles only where likes exist, grouped views capped at 6-tile previews in SQL (ROW_NUMBER/COUNT OVER) with a +N overlay, By-person ordered pinned-first (guests.pinned guarded migration + admin checkbox on the guest edit form) then most-recent-photo-first, live person search over [data-person-section] via the shared HuntFilter word-prefix rule with server-side ?q= fallback, chips unified on the app-wide chip pattern, Show more replaces Older →. Issue-review PASS both (independent Opus); Fable routine pattern: fresh-context design-philosophy self-review PASS, one fix round (jsdom wiring test, rebase, 3 nits), same-reviewer confirm PASS. 513/513 tests, lint/format clean. Header translucency deliberately left for #252 (merges-second rule); CodeQL advisory rate-limiting note unchanged (pre-existing pattern, #180 tracks it).

- 8defea7 — #257 + #258 (PR #262) — design(admin): phone card layouts for the guests and tasks admin pages, shipped as one reviewed batch from the 2026-07-05 design session (the #221 design-language comment is the taste reference). #257: the 8-column guests table (22px name inputs, four columns off-screen at 375px, no search) becomes a card list — name edit form (input ≥ 60% of card, AC-guarded in theme.css), `N pts · D/T tasks` meta line (denominator = all tasks; UNIQUE(guest_id,task_id) + cascade makes done ≤ total invariant), copy-link via `navigator.clipboard` with inline `Copied!` (selectable readonly input kept as fallback), bonus-points form, badge-award select, delete with `data-confirm`; live any-word-prefix name search as a new pure shared `src/public/js/filter.js` (issue corrected on GitHub first: gallery search is server-side substring in `feed.js` `grouped()` — no client helper existed to share, so the AC's rule ships as the new canonical filter); the three setup forms collapse into `<details>` below the list; header buttons share a gapped flex row. #258: the tasks table becomes cards — full-width title input + 3-row textarea, one action row (Save via the HTML `form` attribute · Active toggle · Delete · muted right-justified photo-count link to `/gallery?task=<id>`); `POST /admin/tasks/reorder` gains `direction=top` (single atomic `MIN(sort_order)-1` update); reorder/edit/active redirects anchor to `#task-<id>` (delete deliberately not — the card is gone); the add-task form gains an `Add to top` checkbox. The badge route gained `action=toggle` (server resolves award-vs-remove from actual held state) so the card's badge select is correct with JS disabled, by construction. Reviews: 2 independent Opus issue-reviews, both PASS round 1 (#257 with 1 wording nit). PR review per Fable Pattern 1 (routine UI), one round: fresh-context design-philosophy self-review **FAIL round 1** — the real catch: `.guest-card { display: flex }` beats the UA's non-!important `[hidden]` rule on WebKit, so the live search would have visibly done nothing on iPhones while the jsdom test (asserting the `hidden` property, not computed display) stayed green; fixed with `.guest-card[hidden] { display: none !important; }` + a structural guard test — plus done>total meta edge, dead `.page--wide`, the no-JS badge-action mismatch (→ the toggle rework), and the dead delete anchor; all five fixed → same-reviewer confirm PASS, 0 defects. New devDep `jsdom` for the clipboard/filter DOM tests. 477 tests green pre-rebase, 486 after rebasing over #250/#249 (23 new: 15 guests-ui incl. toggle + [hidden] guard, 8 tasks-ui); lint 0 errors; format clean; verified live at 375×812 (`scrollWidth === 375` both pages, 0 tables, all card controls ≥ 44px, search filters and clears, move-to-top lands on `#task-<id>`); all required checks green both runs; smoke: now in the required-check set and green both runs (no movement from this change). Merge-on-green (merge `a23ccc5`).
- 7d5a7ce — #250 + #249 (PR #260) — fix(tasks)+design(leaderboard): task list v2 + podium ties show every tied guest, shipped as one reviewed batch. #250: root-cause fix for the 566px-wide task page on 375px phones — the nowrap `.task-desc` is now a wrapped 2-line clamp (`document.documentElement.scrollWidth` verified 375 exactly, live in a browser against the demo dataset, which matches the AC's 13 undone / 19 done); `To do · n` / `Done · n` filter chips (solid=active/outline=inactive, 44px), whole-row links (serif title, wrapped description, `+1 pt` from `scoring.POINTS_PER_PHOTO`, chevron), done rows show the guest's own photo with an 18px sage check badge (grey `☐` glyphs, TO DO/DONE pills, and per-row `See photos` links all removed — detail page keeps the gallery link as the one path), default view = all to-dos then the 3 most recent completions (cap computed in the route), `?view=done` full list with no JS required. #249: tied podium slots render every tied guest — ≤3 overlapping 44px linked avatars ringed with page background + `+N` chip, natural-joined linked first names (`Liam, Priya and Noah` / 4+ → `Liam, Priya and 3 more`), `2nd place · 12 pts each` sub-line; every plinth carries a white `1st · 19 pts` label on primary sage (review finding drove the fill choice: the mid-ramp step failed WCAG AA for the very labels curing the blank-rectangle defect); star tile and `T2 · N tied` string deleted, view+CSS+route tie-collapse machinery removed (`podiumGroups` now carries the full member arrays; the view renders precomputed structure verbatim). Reviews: 2 independent Opus issue-reviews, both PASS round 1 (findings: one AC per issue missing an explicit When clause — minor form, non-blocking). PR review per Fable Pattern 1 (routine UI), one round: fresh-context design-philosophy self-review PASS with 2 minors + 5 nits (bar-label contrast; stale collapsed-tile comments; dead `podium-slot-group` hook; view-layer `slice(0,3)`) → all fixed except one judged-acceptable skip (12-line done-row duplication in `tasks.ejs`, visible over a partial's indirection) → confirm PASS. Post-review CodeQL flagged the new test helper's tag-strip regex (`js/incomplete-multi-character-sanitization`, high) — fixed by looping the replace to a fixpoint, re-gated on the new tree, CodeQL green. Deviations recorded on the PR: #249's plan named nonexistent `tests/leaderboard.test.js` (extended `leaderboard-ties.test.js` instead); two pre-#250 tests asserting the removed markup were legitimately updated in-file (`gallery-views.test.js` AC4 → detail pages; `loadtest.test.js` parser fixture → v2 rows, parser itself unchanged). 463 tests green (9 new: 6 tasks-page + 3 podium-tie); lint 0 errors; format clean; all required checks green both runs; smoke no movement (green before, green after). Merge-on-green (merge `4e6b48f`).
- ae29b22 — #198 + #199 (PR #236) — governance(test-quality): coverage gate ON at measured floors + mutation-testing baseline, shipped as one reviewed batch. #198: the 80% thresholds the owner believed were enforced had been commented out since #1; instead of waiting for a suite that clears 80, the gate is ON at the floors measured on `main` @ 485886a — statements 62, branches 53, functions 65, lines 62 (the issue's original floors 82/78/78/82 were measured on stale commit b1cc5ab, before the low-coverage `scripts/` additions of #219/#220/#197 dragged the include-set down ~20 points; corrected on GitHub pre-review, confirmed by the issue-reviewer's exact independent re-measurement). Floor proven live (threshold above measured → exit 1); ratchet note (raise toward 80/80/80/80 via #181, never lower) in the config comment + DESIGN.md. #199: StrykerJS (Apache-2.0, local, `npm run mutation`) over `src/services/**` + `src/routes/auth.js`; baseline **61.06%** (922 mutants: 563 detected / 197 survived / 162 uncovered) in `docs/test-quality.md` with the owner-readable thin-spots list (export.js 16% — Goal D's keepsake path nearly undefended; qr.js 36%; the admin-lockout `>=` boundary at auth.js:238 — the hand-demonstrated missed mutant, now pinned; the badge-REMOVAL guard scoring.js:337 — its award-path twin is fully killed). AC2's three hand probes reproduced exactly (detected / detected / SURVIVED). Two run-breaking config gotchas found and documented: `vitest.related` off (supertest-style tests defeat related-file detection → "No tests were executed"), `symlinkNodeModules` off (native modules crash 0xC0000005 under the sandbox symlink on Windows). Weekly non-required `mutation.yml` (least-privilege `contents: read`) uploads the report; per-PR gating deliberately rejected (signal, not loop cost — rationale in the doc). Issue reviews (independent Opus): #198 PASS round 1; #199 FAIL round 1 (all-structural ACs a broken run could pass; MIT license claim wrong — StrykerJS is Apache-2.0) → behavioral AC2 added pinning the three probes → fresh-instance PASS round 2. System-level tree (`.github/` + DESIGN.md) → Fable Pattern 2, one round: fresh-context design-philosophy self-review FAIL (1 major — the thin-spots list misattributed the surviving badge-guard mutant to `awardSpecialBadge`; the report proves the award guard fully killed and the survivor in `removeSpecialBadge` — plus 1 minor, 2 nits) and independent Opus reviewer-pr PASS (2 minors: missing workflow `permissions`, undeclared Touches; 1 nit) → all fixed → both same-reviewers confirmed PASS on final tree `94c8e745` (2 distinct PASSes + bias-gate). Session note: the first implementation agent (Sonnet) died mid-session with zero output; Fable re-authored the change from scratch at the owner's direction. 454 tests green; lint/format/coverage exit 0; all required checks green on branch and merge. Merge-on-green (merge `1e0b1dc`).

## 2026-07-04

- 76be6af — #222 + #223 (PR #234) — governance(review): security lens charter + PR-checklist gaps, shipped as one reviewed batch (#218 batching rule). #222: `agents/reviewer-security.md`, a conditional lens firing on upload/intake, auth, file-serving/static, or admin-route diffs; four charter questions (reach, leftover-state, unboundedness, error-path leakage); ships advisory per the standard lifecycle, with an escalation exception — a major/blocker finding flags the change `security` and forces the existing ≥3-reviewer bar mid-trial. Dispatch-table row in `standards/adversarial-review-protocol.md` swapped from placeholder to the real charter pointer; `DESIGN.md` records why conditional (cost) and why the escalation exists (a lens finding that can't block is theater). Worked example traced against #196's real touched paths (`src/routes/admin.js`, `src/app.js`'s `/uploads` static mount) — confirmed the lens fires. #223: two additive checklist items on `agents/reviewer-pr.md` — leftover-state (files/rows/pages/URLs left behind by a create/delete/hide/restore/resubmit; evidence #190, #191, #196) and unboundedness (unpaginated/unrated file-serving, list, or query routes; evidence #194) — zero existing items removed or reworded (independently confirmed via `--numstat` 2/0 and `--word-diff` insertions-only). This diff touches `standards/`, a kernel path, so the batch took the kernel/system-level bar (`Get-RequiredBar` = 2) rather than the routine reviewer-charter carve-out: 1 Opus issue-review (batch PASS, 0 defects) + Fable Pattern 2 — fresh-context design-philosophy self-review (PASS, 2 nits: a charter phrasing didn't literal-match its AC's quoted string, and a wording asymmetry between two charters' coverage clauses) plus one independent Opus PR reviewer (PASS, 1 nit: consistency-only note) — 2 distinct PASSes on tree `414cb75`. 454/454 tests green, lint/format clean. All CI checks green; merge-on-green (merge `220a3bd`).
- 5c7af29 — #220 (PR #231) — governance(event-mode): wedding-day freeze switch. `governance/event-mode.json` (`em1`, single writer `tools/set-event-mode.ps1`) arms a pre-declared window in which a `hotfix: `-prefixed commit passes both hooks with no review evidence and no reviewed issue; everything else takes the identical gate at `commit-msg` (pre-commit can't read the message, so it defers; shared body `.githooks/gate-core.sh`). commit-msg keys on flag-FILE presence, not ACTIVE state — the fresh-context design-philosophy review caught that keying on ACTIVE lets the flag expire between the two hooks and skip the evidence gate in BOTH (fail-open); presence-keying makes the gate run at-worst-twice, never zero, and (stated honestly) newly covers merge commits while a flag exists. Its second major killed a Node duplicate of the flag-parse rules: CI's new `event-mode-expiry` job runs `pwsh -File tools/check-event-mode-expiry.ps1`, a thin wrapper over the ONE reader (`tools/event-mode-core.ps1`), red while an expired/invalid flag is in the tree. Retro-review is mechanical, not aspirational: harvest marks `hotfix: `-carrying merged PRs `freeze:true`; `-Clear` refuses until every freeze row since the flag's `created` has a review PASS bound to that commit's tree, then removes the flag. REHEARSED, not hoped: `scripts/rehearse-event-mode.ps1` (scratch repo, real hooks) — arm → hotfix passes → non-hotfix blocks → expiry blocks → `-Clear` refuses naming the sha → retro PASS → `-Clear` succeeds; 6/6 steps green, run twice (pre- and post-fix trees). Suite 454/454 incl. race-seam tests that install commit-msg WITHOUT pre-commit. Reviewed per Fable Pattern 2 (system-level/self-modification, one round): independent Opus reviewer-pr PASS → fresh-context design-philosophy FAIL (the 2 majors above) → fixed → same-reviewer confirm PASS + reviewer-pr re-render PASS on final tree `14394591`. Issue Touches map corrected on GitHub (3 new files named) per the reviewer's minor. Docs: DESIGN.md + protocol § "Event mode (#220)". All checks green incl. smoke and the new event-mode-expiry job's first runs. Merge-on-green (merge `8973359`).
- fda52ea — #228 — governance(ledger): CI appends routed to the protected `ledger` branch after #219's first live harvest run (28730043254) proved a direct push to `main` impossible (GH006: PR required + 6 required checks, enforce_admins; a GITHUB_TOKEN-created PR can't trigger its own required checks, so auto-PR is dead without an owner PAT). `scripts/ledger-push.js` (`--materialize` seeds the working copy from `origin/ledger`; `--push` builds the append commit with git plumbing parented on the branch tip and pushes only `refs/heads/ledger`), workflow rewired materialize→harvest→push-to-ledger, `snapshot-governance.ps1` stats source order `origin/ledger`→HEAD→`no ledger rows`, DESIGN.md updated. Branch `ledger` created and protected (force-push/deletion blocked, admins included — server-side append-only). Reviews: issue-review Opus PASS 0 defects; Fable Pattern 2 PR review Opus PASS, 1 minor (stale header comment) fixed and confirmed by the same reviewer in-round. 429 tests green. End-to-end verified post-merge: the harvester's next run appended this PR's own rows to `origin/ledger`.
- 8517b0a — #219 + #224 — governance(observability+template): committed governance ledger + snapshot tool, shipped as one reviewed batch (#218 batching rule; #224 consumes #219's report). #219: `governance/ledger.ndjson` (committed, append-only; ONLY writer is CI post-merge — a row is never part of the tree it describes), `.github/workflows/ledger.yml` harvest job (self-loop guard, `design-reversed` label trigger, concurrency group), `scripts/ledger-harvest.js` pure harvest functions (fenced `governance-ledger` PR-comment → `reviews` verbatim, else `[]`; `gl1-governance` rows mirror `verdict-core.ps1`'s kernel regexes; `gl1-reversal` rows), `tools/governance-report.ps1` (per-role totals/rounds/reversals; literal `no ledger rows` on empty), commit-msg sanctioned-appender exemption (sole path + `ledger: ` prefix, test-pinned). #224: `tools/snapshot-governance.ps1` (annotated tag `governance-v<N>`, refuses dirty tree/tag collision, exports governance surface + `stats.txt`; template publication stays a manual owner step). Reviews: both issue-reviews Opus PASS; Fable Pattern 2 PR review Opus PASS (0 blockers/majors; 2 minors + 2 nits recorded — reviews-empty rows invisible in rounds-per-issue section; first-json-fence-wins inside a single comment). 424 tests green (22 new). Known follow-up shipped same-day as #228 (delivery path).

- 02d54a5 — #218 + #201 — governance(review): kernel/experimental review split, review batching, advisory-lens lifecycle, review-dispatch checklist (#218) + routine PR panel cut to exactly 1 reviewer with the design-philosophy gate unchanged and the bias-gate audit moved to once-per-distinct-briefing-template (#201) — shipped as ONE reviewed batch per the batching rule #218 itself introduces (owner pairing note on #201; #201's graduate-after satisfied: #197 merged, `smoke` required). Mechanics: `$EXPERIMENTAL_PATH_REGEX` (`^agents/reviewer-[^/]+\.md$`) carves reviewer charters down to bar 1 in `Get-RequiredBar` while every other governing-artifact path stays bar 2 (four new tests pin both AC directions plus mixed trees and nested lookalikes); the protocol gains §§ "Review batching", "Advisory-lens lifecycle" (a trial lens cannot block a merge; a major/blocker security-lens finding escalates to the security-flagged bar), and "Which reviews does this change need?" (5-row path-to-lens dispatch table); routine round 1 = 1 PR reviewer + design-philosophy, both-must-PASS (round-ledger evidence: ~9 panels across three sessions, zero verdicts flipped by panel width; design-philosophy caught every consequential defect); security (≥3) and system-level (2-independent both-PASS) bars unchanged. Kernel batch → one issue-review pass covering both issues (independent Opus, PASS ×2, 1 nit) and Fable Pattern 2 PR review, one round: the independent Opus reviewer PASSed round 1 with 0 defects, while the parallel fresh-context design-philosophy pass FAILed with 3 majors + 2 minors + 1 nit (the docs-only dispatch row would have exempted charters from the very design-philosophy gate the table sits beside; the Fable-vs-carve-out interaction was undefined — resolved: a Fable-authored charter edit is self-modification and takes Pattern 2 regardless of the bar-1 carve-out; the escalation exception cited an all-PASS bar where the tier table says 2-of-3; plus ledger tense, `skills/spawn-adversarial-review.md` cadence drift, `WHAT-IT-CHECKS.md` panel wording) → all six fixed → both reviewers confirmed PASS on final tree `3ee88c11`. Recorded non-blocking follow-up candidate: the Fable-charter rule is policy-prose — the gate has no implementer identity, so a charter-only tree still validates at Required=1 against a lone self-cert record (honestly visible as `role: 'self-cert'`). 402 tests green (4 new); lint 0 errors; format clean; smoke green on both CI runs — first merge with `smoke` as a required check, no movement (green before, green after). Merge-on-green (PR #225, merge `1afbd91`).

- 8dc74ad — #200 — feat(governance): read-only freshness check — the primary checkout now tells the owner when it is behind origin/main. Build sessions merge on GitHub from isolated worktrees and nothing ever pulled those merges back into `C:\wedding-scavenger-hunt`, so on 2026-07-03 the owner reviewed a checkout 32 commits behind without knowing — the one human check in the system pointed at stale evidence. New `tools/check-freshness.ps1` (mirrors the `assert-worktree.ps1` pure-check shape): `git fetch --quiet origin` then `git rev-list --left-right --count origin/main...HEAD`; behind → literal "N commits behind" + exit 1 (plus a local-commits-ahead note when applicable); current → literal "up to date" + exit 0; fetch failure → fails closed ("could not verify freshness", exit 1, no comparison) so an offline run can never show false confidence. Read-only by construction (AC3): fetch and rev-list are the only git commands executed; `git pull` appears only in output prose. Surfaced where the owner starts work (AC2): first item of `README.md` Quickstart, step 0 of `docs/test-plan.md` Setup (the wedding dress-rehearsal now starts by proving it runs current code), step 0 of `skills/session-brief.md` Procedure. System-level tree (`tools/` + `skills/` match `$SYSTEM_PATH_REGEX`) → Fable Pattern 2 (#207), one round each: issue-review one independent Opus reviewer PASS, 0 defects; PR review one independent Opus reviewer FAIL — the consequential finding was the offline path itself (fetch fails → script compared against the stale tracking ref and printed an authoritative "up to date"/exit 0, the exact false signal the issue exists to prevent), plus HEAD-vs-main comparison undocumented — fixed fail-closed + rationale comments → same reviewer re-executed all four paths (behind / up-to-date / behind+ahead / offline) in throwaway repos and confirmed PASS on tree `a81dbb8`, 0 open blockers/majors (accepted won't-fix nit: "1 commits behind" grammar is pinned by AC1's literal-phrase contract). Both PASSes on the final tree per the system-level `Required=2` bar (independent `reviewer-pr` + capped self-cert record) + bias-gate self-cert evidence. 398 tests green (0 new — no JS added); lint 0 errors; format clean; smoke unchanged (green both halves, as since #193). Merge-on-green (PR #216, merge `8dc74ad`).

- a9ba136 — #193 — fix(badges): rebuild all nine badges on the green/white design-system palette; COMPLETIONIST/MOSTPHOTOS art + one shared seed catalog. The #80 engine shipped two computed badges whose presentation never landed: `scripts/seed.js` seeded `COMPLETIONIST`/`MOSTPHOTOS` with art_paths that had no files (guest home / leaderboard / public profile rendered a broken image at the exact reward peak), and `scripts/seed-event.js` carried its own hand-copied 7-row `BADGES` array, so every dress rehearsal, load test, and demo ran with the engine silently inert. Owner-expanded scope (2026-07-04): the seven existing badges were also off-brand — pastel pinks/purples/blues/yellows on cream against the wedding's forest-green-on-white design system — so all NINE were rebuilt as one Fable-designed family on the six `DESIGN_SYSTEM.md` § 2 tokens: white disc, `#467058` 4px ring, `#aebbb2` inner hairline, one bold solid emblem each (5-petal bloom, tied bouquet, flower row, sunrise+birds, camera, heart-in-crowd, rosette-heart medal, laurel+check, photo-stack+crown), legible at the 24px leaderboard size (verified by rendered contact sheets at 240px/24px, by implementer and reviewer independently). Catalog extraction: new data-only `scripts/badge-catalog.js` is the single source both seeds insert from (AC4) — seed-event's duplication rationale (seed.js's require-time side effects) dissolved by keeping the shared module side-effect-free. New `tests/badge-catalog.test.js`: art-exists iterates the SEEDED DB rows (any future badge without art fails CI), palette conformance iterates the badge DIRECTORY (allowed: six tokens + `#fff`/`none`/`currentColor`; named colors, `rgb()`/`hsl()`, `url()`, `style` attr/`<style>`, gradients/patterns, and SMIL `animate`/`set` all fail closed, both quote styles), plus the structural single-catalog check. `tests/badge-engine.test.js` extended (last state-mutating block): wipes the catalog, rebuilds via `ensureBadgeCatalog` asserting `inserted === 9`, event-seeds 5 guests, full-coverage guest gains `COMPLETIONIST`, and every visible-submission leader holds `MOSTPHOTOS` (tie-safe set membership). Fable Pattern 1 (#207), routine tier (reviewer confirmed no `$SYSTEM_PATH_REGEX` path touched), one round each: issue fresh-context review PASS with 3 minors (AC2 paint-syntax evasion routes closed into the AC text; AC3 in-process-seeding ambiguity pinned; diagnosis hex enumeration corrected) → fixed → same-reviewer confirm PASS, 0 remaining; code fresh-context design-philosophy self-review bound to tree `a261f662` FAIL with 1 major (palette regex matched only double-quoted attributes — reviewer demonstrated a single-quoted `fill='hotpink'` passing) → fixed (both quote styles + SMIL block) → same-reviewer replayed the evasion probes and confirmed PASS, 0 remaining. 398 tests green (6 new); lint 0 errors; format clean. **Smoke movement: the #197 gate's badge-art audit — red on `main` since the gate shipped, solely for these two missing SVGs — now PASSES on the PR branch; with #187's avatar probe already green, this was the last red half of the smoke job.** Merge-on-green (PR #214, merge `e89fc06`).

- cbd8a72 — #188 — fix(photos): HEIC rejected at intake with actionable copy — no more dead-end retry loop for iPhone/Samsung guests. The pipeline accepted `image/heic`/`image/heif` and stored `.heic` originals on the strength of a comment claiming "sharp reads HEIC originals fine via its bundled libvips" — false for the prebuilt binaries this app runs on: `sharp.format.heif.input.fileSuffix === ['.avif']` (AV1-only libheif; HEVC excluded for patent licensing; re-verified against installed sharp 0.35.2 during the build AND independently by both reviewers). A real HEVC HEIC upload flowed multer-accept → original on disk → `makeThumb()` throw → `thumb_failed` → "Sorry, we could not save that photo. Please try again." — a retry loop that can never succeed, surfacing on Files-app picks, third-party browsers, and Android/Samsung Chrome (iOS Photos-picker transcoding hides it in casual testing). Fix: HEIC/HEIF removed from `ALLOWED_MIME_TO_EXT` / `ORIGINAL_RE` / `THUMB_RE`; the shared `fileFilter` now rejects those mimetypes with dedicated copy ("That photo format (HEIC) can't be uploaded here. Take a screenshot… or switch your camera to 'Most Compatible'…") — built in the one place the rejection is decided; the submit route already flashes `err.message`, so no route change. Blast radius (intentional, called out in the issue): the filter is shared with avatar intake, so HEIC avatars get the same actionable rejection instead of dying inside `saveAvatar`. Every HEIC comment in `photos.js` corrected with an explicit do-not-re-add guard; stale `sharp@0.33.5` note fixed; regex copies/doc-comments updated in 5 test files; `PLAN/05-photos.md` gains a superseded-by-#188 note; `docs/test-plan.md` gains X2b (real-iPhone Photos+Files pickers and real-Android dress-rehearsal uploads — success or actionable rejection, never the dead end). New `tests/heic-rejection.test.js`: heic + heif rejection (no row, no orphan file in `UPLOADS_DIR`, redirect + `photo format`/`screenshot`/`Most Compatible` flash) and the JPEG happy path (row + thumb 200). Issue body pre-review re-verification recorded on GitHub (installed-sharp evidence updated from the original 2026-07-03 report). Fable Pattern 1 (#207), one round each: issue fresh-context review PASS with 3 minors + 1 nit (AC2 rewritten to bind literal tokens; avatar blast-radius sentence added; comment coverage widened to every HEIC reference; stale sharp version noted) → fixed → same-reviewer confirm PASS, 0 remaining; code fresh-context design-philosophy self-review bound to tree `12d388db` PASS with 2 minors fixed in-round (stale `photos.js:423,426` line-number cross-refs in `event-fixture.test.js` replaced with filename-only pointers; PLAN/05 supersession note) → same-reviewer confirm PASS, 0 defects. Mid-build encoding near-miss caught by diff-stat review: a PowerShell 5.1 `Get-Content`/`Set-Content` comment edit mojibake'd em-dashes in 4 test files — reverted and redone via explicit UTF-8 `ReadAllText`/`WriteAllText`. 383 tests green (3 new); lint/format clean; required checks green; smoke unchanged (red solely on pre-existing #193 badge art; hostile-avatar probe still PASSes). Merge-on-green (PR #212, merge `cbd8a72`).

- f8dbde9 — #187 — fix(onboard): corrupt avatar upload no longer crashes the app. `POST /onboard` mounted `photos.uploadAvatar` as bare route middleware and awaited `saveAvatar` with no try/catch — one undecodable file labelled `image/jpeg` rejected sharp's promise, Express 4 does not catch async-handler rejections, and the whole process died (full outage until a human restart; the exact crash class the #197 smoke gate reproduced RED on `main`). The route now invokes `uploadAvatar` with an explicit callback (the `guest.js` `/me/edit` pattern): a fileFilter/size rejection or a sharp decode failure re-renders onboarding with a friendly 400 (`AVATAR_ERROR` binds tests to `could not use that photo`; submitted name preserved; guest NOT marked onboarded), and the entire async callback body is wrapped in try/catch routing anything unexpected (e.g. a DB write failure) to the global error handler via `next(e)` — no remaining unhandled-rejection window on the route. Event runbooks (`README.md` Quickstart, `docs/test-plan.md`) now start the server via new `npm run serve` → `scripts/serve-resilient.js` (auto-restart ~1s after a crash; bare `npm start` kept for debugging). New `tests/onboard-avatar.test.js` pins AC1–AC4 (corrupt bytes → 400 + server alive; name pre-filled + friendly copy; name-only retry onboards; PDF → friendly re-render with zero files written). Issue body corrected on GitHub pre-review: its "no fileFilter" claim was stale — the shared `uploadAvatar` has had one since #122; the real gap was the bare mount's error path. Fable Pattern 1 (#207), one round each: issue fresh-context review PASS with 2 minors + 1 nit (header `# N —` form; AC2 hedge removed and exact copy pinned; `docs/test-plan.md` added to AC5/Touches) → fixed → same-reviewer confirm PASS; code fresh-context design-philosophy self-review bound to tree `795be7c9` PASS with 1 minor + 3 nits (minor fixed in-round: whole-callback try/catch closing the residual same-class crash window on the DB writes; lint nit fixed via optional catch binding; 2 nits accepted: socials not re-filled on the error re-render (matches the pre-existing missing-name path), `docs/loadtest.md` bare `npm start` outside Touches) → same-reviewer confirm PASS, 0 open blockers/majors. 380 tests green (3 new); lint/format clean; required checks green. Smoke movement: the hostile-avatar probe now PASSES (was the #187 RED half of the gate); the job stays red solely on the pre-existing #193 badge-art gap (`COMPLETIONIST`/`MOSTPHOTOS` SVGs), already tracked. Merge-on-green (PR #210, merge `f8dbde9`).

- 9a2c628 — #207 — feat(governance): empirical smoke gate (#197) + Fable review patterns — the owner-decided same-day narrowing of #203. New `scripts/smoke.js` boots the real app in-process on an ephemeral port against a `seed-event`-seeded throwaway DB and verifies observed behavior: admin login page, guest sign-in from a real seeded token, the signed-in hot paths (`/`, `/gallery`, `/leaderboard`, `/feed`), a hostile non-image avatar POST (must 4xx with no unhandled rejection and a live server after — the #187 class), and a badge-art referenced-asset audit over BOTH seed catalogs (`seed-event.js`'s live DB plus `seed.js` seeded into a scratch dir — the #193 class lives only in the latter). Pure helpers exported and unit-tested in `tests/smoke-harness.test.js` (the pre-existing `tests/smoke.test.js` config-keys test untouched). New CI `smoke` job runs on every push/PR; deliberately NOT a required check — on ship day it is correctly RED on `main`, reproducing exactly open #187 (`unhandled rejection: Input buffer contains unsupported image format`) and #193 (`COMPLETIONIST`/`MOSTPHOTOS` SVGs missing): the gate demonstrating the provenance-vs-behavior gap it exists to close. Promotion is one command once green on `main`: new `-RequireSmoke` switch on `tools/apply-branch-protection.ps1` (two-stage promotion recorded in `DESIGN.md` § "Empirical smoke gate (#197)"; #197 stays open to track the flip). Fable review patterns (#207): routine Fable work = fresh-context self-review under the design-philosophy charter; system-level / security-flagged / self-modification Fable work = exactly ONE independent reviewer; every Fable review capped at one round (report → fix → same-reviewer confirm; unresolved consequential defects halt to the owner). `tools/persist-self-certification.ps1` tree mode now capped at ONE record, so the system-level `Required=2` bar is mechanically unsatisfiable by self-certification alone — the second distinct PASS must come from `tools/persist-review.ps1`; `tests/persist-self-certification.test.js` rewritten to pin both directions (self-cert-alone blocked; independent+self-cert+bias-gate authorized). `standards/adversarial-review-protocol.md` § "Fable review patterns (#203, narrowed by #207)", `DESIGN.md`, and `CLAUDE.md` all rewritten to the decided scope; issue #48 rescoped per #207 AC2 (five-check reality, no ≥1-approval demand, explicit self-cert-evidence rule). Process (the new patterns applied to their own creation): issue-reviews for #197 and #207 each ran one independent Opus reviewer, one round — #197 FAIL (6 defects: required-check freeze, AC3 not actually catching #193, test-file clobber, unpinned onboarded state, vague plan step, path composition) → fixed → confirm PASS; #207 FAIL (no implementation plan, no behavioral AC, under-specified decision clauses) → fixed → confirm caught 3 revision-introduced defects (dropped user story, unenforced co-ship coupling, AC2↔AC4 contradiction) → reviewer-prescribed fixes applied → verified PASS. PR review: one independent Opus reviewer on the staged tree — PASS with one nit (comment named the wrong test file; fixed as prescribed); a follow-up 30s-vitest-timeout fix for the 4-subprocess gate test (flaked at the 5s default on a loaded PR runner; `04009bc`) confirmed by the same reviewer as assertion-neutral. 377 tests green; lint/format clean; required checks green (smoke red by design, non-required). Merge-on-green (PR #208, merge `f171573`).
- 144e4b4 — #203 — feat(governance): add Fable full self-certification path (all tiers, permanent, owner-directed). Fable-authored issues and PRs may now be committed on Fable's own recorded PASS at every risk tier — routine, system-level, security-flagged/high-stakes, and self-modification — with no independent reviewer required, indefinitely; CI (lint/format/tests) remains the only automated gate on Fable-authored work. Every other implementer remains fully bound by the unmodified Independence and Self-modification-bar rules. New `tools/persist-self-certification.ps1` (dual issue/tree-mode writer; `-Model` locked to `fable` via `ValidateSet`; mints `k` distinctly-tagged `reviewer_id`s `fable-self-1`…`fable-self-k`, `role: 'self-cert'`, `verdict` fixed `PASS`) satisfies `Reduce-Verdicts`' existing distinct-id count at any `Required` bar — including the system-level `Required=2` — with zero change to the shared counting kernel; a new `-SelfCertify` switch on `tools/persist-bias-gate.ps1` clears the system-level bias-gate check the same way. Recorded as a dated, owner-directed decision in `DESIGN.md` (§ "Fable full self-certification exception (#203)"), modeled on the existing #80 and 2026-07-02 merge-policy owner-direction precedents — this is a deliberate, explicit loosening of the independence guarantee, not a claim that independence was unnecessary. Issue-review took 3 rounds (rounds 1–2 FAILed on acceptance criteria that misdescribed how `validate-verdict.ps1`'s auto-bar/bias-gate path and `Required` defaulting actually behave); the additive `reviewer-architecture` gate FAILed once (no recorded owner decision authorizing the amendment — fixed by adding the DESIGN.md record) and the additive `reviewer-design-philosophy` gate FAILed once (schema-label literal `irev1`/`rev1` duplicated across the new writer and its readers — fixed by extracting shared `$SCHEMA_IREV1`/`$SCHEMA_REV1` constants into `tools/verdict-core.ps1`, reused by all three writers, preserving each writer's independent write-path). System-level change: two independent `reviewer-pr` PASSes plus bias-gate PASS obtained on both the implementation tree and the follow-up DESIGN.md-only tree. 368 tests green; lint/format clean. Merge-on-green.

## 2026-07-03

- 6b07f38 — #88 — fix(home): remove the contradictory next-badge line + re-base the progress bar on tasks + guard many-badge rendering. The home screen showed `<N> more tasks to your next badge (at 5/10/15 tasks)` directly above a `<X> of <Y> tasks complete` bar — two denominators that read as contradictory (with 6 seeded tasks the 15-task badge is unreachable). Removes the badge-threshold progress machinery from `src/routes/guest.js` (`nextThreshold`/`prevThreshold`/`remainingToNext`/the `AUTO_THRESHOLDS` import) and the next-badge label block (both the "N more…" line AND the "you've earned every auto badge" branch) from `guest-home.ejs`, leaving one task-progress framing. The progress bar is re-based on task completion: `progressPercent = totalTasks===0 ? 0 : Math.max(0, Math.min(100, round(completedTasks/totalTasks*100)))` — clamped to `[0,100]` so a completed-then-deactivated task (`completedTasks` uses the canonical no-`is_active` count while `totalTasks` counts active tasks only) cannot push `aria-valuenow` past `aria-valuemax="100"`. Held badges still render unchanged on home, the leaderboard, and the public profile — the new #80 engine badges (`COMPLETIONIST`/`MOSTPHOTOS`) flow through the same `guest_badges` loops with no new render code; and the home `.badge-grid` (CSS grid, 3–4 cols) wraps a large collection across rows, so a guest holding 10+ badges (per the owner's UI concern) is shown in full with no truncation — the dedicated-area counterpart to #78's space-constrained leaderboard-row cap. New `tests/badge-frontend.test.js`: AC1 (`2 of 6`, no `to your next badge`), AC2–AC4 (badges still render on home/leaderboard/profile + the `No badges yet` empty state), AC5 (`aria-valuenow="33"` for 2/6, and `"100"` clamped when completed>total), AC6 (a 12-badge guest renders all 12 names on home). Wave 3 / Lane G. Goal A (the app explains itself — one clear number) + Goal B (legible status). Depends on #122 (shared guest.js). Routine code: issue-review PASS; bias-gate PASS before fan-out; PR round-1 two independent `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL on two majors (the re-based `progressPercent` dropped the old `[0,100]` clamp → `aria-valuenow` could exceed `aria-valuemax` when a completed task is later deactivated; and a stale handler doc comment still describing "a progress bar toward the next auto badge") → clamp restored, comments corrected, plus a new inversion-sensitive test that seeds `completedTasks(2) > totalTasks(1)` and asserts the clamped `aria-valuenow="100"` (would be `200` unclamped); round-2 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS (0 blocking; nits only). 358 tests green; lint/format clean. Merge-on-green.
- 0ad3c9d — #149 — test(leaderboard): pin the reconciled per-photo (#89) + tiebreaker (#78) ranking. The combined guard neither #89 nor #78 provided alone: #89 tested per-photo points with no ties, #78 tested ties on plain points; #149 tests a tie CREATED by per-photo bonus and RESOLVED by the tiebreaker — the exact SELECT/ORDER BY interaction a bad merge of the two would break. Confirmed `src/services/scoring.js` `leaderboard()` already carries both #89's three-term formula (`COUNT(s.id) * POINTS_PER_PHOTO + COALESCE(SUM(s.photo_bonus),0) + g.bonus_points`) and #78's `MAX(s.created_at) AS last_submission_at` + `ORDER BY points DESC, (last_submission_at IS NULL) ASC, last_submission_at ASC, g.name ASC, g.id ASC` — both terms intact, nothing dropped, so `scoring.js` is left byte-for-byte unchanged and the PR is the test alone (confirm-only). New `tests/leaderboard-reconcile.test.js`: guest A (two visible subs, `photo_bonus` 1+2 → total 5) ties guest B (five visible subs → total 5), and A's latest submission is seeded EARLIER so the tiebreaker orders A before B with both showing an identical `T1` label; guest C (one sub `photo_bonus` 4 + `guests.bonus_points` 3 → total 8) proves the two bonus columns sum independently. The tie guests are deliberately named so the `g.name ASC` fallback would order them WRONG — the test only passes if the real `created_at` tiebreaker fires; dropping `SUM(photo_bonus)` drops A to 2 and fails AC1. Wave 3 / Lane G. Goal B (trustworthy standings; no human verifies the leaderboard by eye, so this automated guard is the gate). Routine code (a test + a confirm-only source file): issue-review PASS; bias-gate PASS before fan-out; PR round-1 two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS (0 blocking). Two reviewers converged on one out-of-scope observation — no `taken_down=1` row with a nonzero `photo_bonus` — accepted as outside #149's stated ACs and already guarded by #89's own AC6. Issue Context prose corrected post-review (it had named the per-photo column `submissions.bonus_points`; the real column is `submissions.photo_bonus`, which the code/test used correctly). 353 tests green; lint/format clean. Merge-on-green.
- 4ee36ed — #78 — fix(leaderboard): reconcile podium with ties + cap row badges. Rank is computed once in `src/routes/community.js` (standard-competition "1224") and shared by the podium and the full standings list, so they can never contradict (the root bug was `rows.slice(0,3)` on the podium vs standard-competition rank in the list). The route now emits `podiumGroups` (rank 1/2/3 in podium display order, each `{rank, rankLabel, members, isCollapsedTie}` where `isCollapsedTie = members.length>=3`) + `showPodium` + per-row `rankLabel`; `leaderboard.ejs` renders that structure and no longer re-buckets rows or reconstructs `T{rank}` (design-philosophy fix: tie definition + label live in one layer). Ties share a `T{rank}` label; a 3+-way tie collapses to a `T{n} · {count} tied` tile (styled for rank-1 first-place ties too); a 2-way tie shows both plinths; an all-tied field (`distinctRankCount==1`) suppresses the podium behind an `everyone's tied` banner. `scoring.leaderboard()` gains `MAX(s.created_at) AS last_submission_at` and `ORDER BY points DESC, (last_submission_at IS NULL) ASC, last_submission_at ASC, g.name ASC, g.id ASC` — orders rows within a tie (earliest-to-the-score first; no-submission guests last) without changing the shared rank label; the #89/#80 points SELECT formula is untouched. Per-row badges cap at `config.LEADERBOARD_BADGE_CAP` (8) with a `+K` overflow chip so a guest holding 10+ badges (per the owner's UI concern) cannot overflow the fixed-height row; the full standings list is labelled "Full standings" and commented so the podium reads as a highlight (tied guests collapsed on the podium remain individually visible below). New `tests/leaderboard-ties.test.js` (AC1–AC6, inversion-sensitive: the `[5,4,3,3,3,3,1]` field → labels `1,2,T3,T3,T3,T3,7`). Wave 3 / Lane G. Goal B (standings guests trust). Routine code: issue-review PASS (AC1 arithmetic corrected mid-build — `[5,4,3,3,3,3,1]` with four tied at rank 3 makes the next distinct rank `7`, not the issue's original `6`; the implementer independently stopped on this discrepancy per its stop-and-report rule before it was fixed); bias-gate PASS before fan-out; PR round-1 two independent `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL (information-leakage: tie definition + `T{rank}` label duplicated route↔view) → centralized in the route; round-2 fresh `reviewer-pr` PASS + design-philosophy FAIL again (obvious-code: full list now includes ranks 1–3 with no explanation of the podium-is-a-highlight intent) → added a "Full standings" heading + invariant comment, fixed an inverted break comment, consolidated a drifted comment; round-3 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS (0 blocking; nits only). 350 tests green; lint/format clean. Merge-on-green.
- 73d225e — #80 — feat(badges): metrics-driven badge engine — computed, transferable, custom. Generalizes `recomputeAutoBadges` into a rule engine: a new `src/services/badges.js` code-keyed registry of `COMPLETIONIST` (type `metric`, one-time — held when the guest has a visible submission for every `is_active=1` task, auto-revoked when a new active task appears) and `MOSTPHOTOS` (type `transferable`, steal-able — the guest(s) with strictly the most visible submissions; ties share it; `max===0` on a fresh DB holds it for no one). `scoring.recomputeBadges(guestId)` runs the unchanged `auto` threshold pass plus the `metric` pass; `recomputeTransferableBadges()` diffs the holder set and rewrites only `awarded_by='system'` rows; both are folded behind ONE `recomputeAfterSubmissionChange(guestId)` seam wired at the two real service sites (`submissions.js` submit, `photos.js` hide/restore inside its existing `db.transaction`, nested via SAVEPOINT) so a future mutator can't half-adopt the ordered pair. Admin-defined `custom` badges: a `POST /admin/badges` create route + form in `admin-guests.ejs`, with the award guard widened to `ADMIN_AWARDABLE_TYPES={special,custom}` in `scoring.awardSpecialBadge`/`removeSpecialBadge`/`createCustomBadge` AND the `admin.js` badge route — `metric`/`transferable` stay system-owned and are refused. Badge identity stays the existing `code` column (no forked `rule` key). Guarded, idempotent `badges.type` CHECK-widening migration (`ensureBadgeTypeCheckWidened`, rebuild-copy-drop-rename in a transaction with FK toggled off, no-op on second boot) so a pre-existing `data/app.db` migrates without error. `DESIGN.md` updated to record the owner-directed custom-badge reversal and the five-value type vocabulary (`auto`/`special`/`metric`/`transferable`/`custom`). New `tests/badge-engine.test.js` (AC1–AC8, inversion-sensitive: grant/auto-revoke, steal+tie, taken-down exclusion, idempotency, admin-badge-untouched, custom survives + system-type refusal at service and HTTP layers, seeded catalog, migration guard). Wave 3 / Lane G (standings). Goal B (badges confer status, pull guests back). **System-level** (edits `DESIGN.md`): issue-review PASS (round-1 FAIL on a stranded AC5 refusal `When` → split) + `reviewer-architecture` PASS (round-1 FAIL on a forked `code`-vs-`rule` identity, threshold-badge coverage gap, `art_path` ambiguity, and an omitted `scoring.js` guard surface → all resolved before implementation); bias-gate PASS with a tree-bound artifact before fan-out; PR round-1 two independent `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL on three majors (unused `db` param = information leakage, a comment describing a non-existent SQL `HAVING` guard, and the two-call obligation duplicated across both mutators) → fixed via the single seam + honest signatures + corrected comment; round-2 two fresh independent `reviewer-pr` + fresh `reviewer-design-philosophy` all PASS (0 blocking; nits only) on the exact committed tree. 344 tests green; lint/format clean. Note: the worktree was cut from a stale local `main` (20 commits behind `origin/main`); reset onto `origin/main` (carrying #89/#122) before any work. Merge-on-green.
- 1db0d6f — #79 — feat(ux): style the photo/avatar upload control (replace the raw "Choose File" button). Replaces the browser-default file picker across all three guest upload forms (task photo, profile-edit avatar, onboarding avatar) with ONE shared partial `src/views/partials/upload-field.ejs`: a `.visually-hidden` native `<input>` (its `name`/`type`/`accept`, and the task form's `capture`/`required`, all preserved) paired with a styled `.btn` `<label>` reading "Choose a photo…" and an `.upload-filename` span. `.visually-hidden` hides via off-screen `clip-path` (never `display:none`), so required-file validation, focus, and tab order still work. `src/public/js/upload-filename.js` owns the feature end to end: a pure `uploadLabelText(fileName)` (returns the filename, else the single `PLACEHOLDER` "Choose a photo…") exported for the Node test, plus DOM wiring (guarded by `typeof document`) that echoes the picked filename on `change` and degrades gracefully with no JS. The existing `/js/upload.js` image preview still binds via the preserved input `id`s (`photo`/`avatar`). New `tests/upload-label.test.js` asserts `uploadLabelText` for `"sunset.jpg"`/`""`/`null`/`undefined` + an inversion-catcher. Wave 2 / Lane E (brand+onboarding), footprint disjoint from Lane F. Goal A (feels finished, explains itself). Routine code: issue-review PASS (re-run in-worktree; the shared helper must be `module.exports`-importable, which the AC steers); bias-gate PASS before fan-out; PR round-1 two independent `reviewer-pr` PASS; a first cleanup removed a dead `.file-input` CSS rule and fixed a specificity leak that partially defeated `.visually-hidden`; `reviewer-design-philosophy` then FAILed on a shallow separate helper module + a cross-file `window` dependency + duplicated placeholder copy → fixed by merging the pure helper into `upload-filename.js` (one cohesive module, exported for the test; no `window` global; `PLACEHOLDER` single-sourced), with the one remaining, AC-mandated no-JS label mirror documented in the partial; round-3 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS (0 blocking; nits only). Rebased onto #89 (`d6e6126`, disjoint code) with a fresh `reviewer-pr` re-confirmation (328 tests green on the integrated tree); then re-rebased onto #178 (`2ae1360`, a BUILDLOG-only sha correction — no code change, the 7-file diff byte-identical to the reviewed tree) before merge. Merge-on-green.
- ef3635f — #77 — fix(brand): use "Lilly & Axel" everywhere, remove "Lillian". Canonicalizes the couple's name to the owner-decided `Lilly & Axel` across the guest-facing brand strings and the repo's own docs: the header `.brand-text` (`header.ejs`), the `<title>` default (`head.ejs`), and the `404`/`error`/`maintenance` page titles; the `theme.css` header comment; the `package.json` description; and `CLAUDE.md` + `docs/north-star.md` + `docs/product-brief.md` — removing every `Lillian` (the formal spelling) and the one-L `Lily` from those docs. The header stays CSS-uppercased (`.brand-text { text-transform: uppercase }`), so only the source words changed — no hard-coded uppercase literal. New `tests/brand.test.js` renders real pages via `loadApp()` and asserts `GET /gallery` and the 404 page each contain `Lilly` and `Axel` and never `Lillian` (falsifiable — `lilly`/`lillian` are distinct substrings). `docs/design-system/` (the owner brand asset that deliberately records `Lilly & Axel (formal: Lillian & Axel)`) and `docs/RESUME-STATE.md` are out of scope. Wave 2 / Lane E (brand+onboarding). Goal A (feels finished; one couple name everywhere). **System-level** change (edits `CLAUDE.md` + `docs/north-star.md`, on the governing-artifact surface): issue-review + `reviewer-architecture` PASS (both confirmed the edits are confined to the brand string, no governing rule touched); bias-gate PASS with a tree-bound recorded artifact; two independent `reviewer-pr` both PASS on the staged tree (bar=2), each verifying AC1 via `npm test`, AC2 via `git grep` (no `Lillian` in `src/`+`config.js`), and AC4 per file. design-philosophy gate N/A (brand-string/typo-class content change; no module/interface design surface). Accepted minor: AC1's admin-login example page is not directly asserted, but AC2 guarantees no `Lillian` anywhere in `src/` and it renders the same brand partials. Rebased onto #87 (`da6911f`; #87 independently added a `/admin/comments` link to the same `header.ejs`, which auto-merged with the brand rename on a different line) with a fresh two-independent-`reviewer-pr` re-review + a fresh tree-bound bias-gate on the exact rebased tree (315 tests green on the integrated tree) before merge. Merge-on-green.
- eee2acc — #89 — feat(feed): per-photo points — base 1 + task-master bonus, counted on the leaderboard — a guarded `submissions.photo_bonus` column (PRAGMA-checked `ALTER TABLE`, idempotent on re-boot; named `photo_bonus` deliberately DISTINCT from the existing `guests.bonus_points` so the per-photo and per-guest levers never collide), a per-photo points display in the feed (`1 + photo_bonus`), an admin `POST /admin/photos/:id/points` (absolute set, non-negative-integer regex guard — not the additive per-guest delta), and the reward loop the owner confirmed: per-photo points COUNT on the leaderboard and profile. New total = `completed*POINTS_PER_PHOTO + SUM(visible photo_bonus) + guests.bonus_points`; the "1 base" lives once as `scoring.POINTS_PER_PHOTO`, surfaced via `scoring.photoPoints()` and shared by the feed display, `getPoints`, and the `leaderboard()` query (single scoring authority, #104) — no bare literal at a call site. `attachPhotoPoints` mirrors #86/#87's `attachLikeCounts`/`attachComments` (keyed off already-visible ids; feed.js keeps sole ownership of the `taken_down = 0` rule). New `tests/per-photo-points.test.js` (AC1–AC6, inversion-sensitive; the AC1 idempotency test binds to db.js's exported `ensurePhotoBonusColumn` guard) and the existing `tests/scoring-single-authority.test.js` updated to the three-term formula (visible `photo_bonus = 3` counted, taken-down `photo_bonus = 9` excluded → 8). Wave 2 / Lane F. Goal B (instant reward + status). High-stakes (leaderboard correctness): issue-review PASS (with the load-bearing `bonus_points` → `photo_bonus` rename applied before review); PR round-1 three independent Opus `reviewer-pr` unanimous PASS; then four converging fix cycles — a stale db.js migration comment + a dead-code AC1 idempotency assertion (minors), a still-tautological AC1 idempotency test (major) plus the `1 + photo_bonus` base duplicated across the route and scoring (design-philosophy information-leakage major), the existing scoring test not updated to the new formula (AC7 major), and the idempotency test bound to an inline copy rather than db.js's real guard — each fixed and re-reviewed with fresh reviewers; final fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS (0 blocking). 321 tests green. Depends on #84 (feed), #86, #87. Merge-on-green.
- 2500d15 — #122 — refactor(avatars): one avatar-intake path for onboarding and profile-edit. `POST /onboard` (`auth.js`) and `POST /me/edit` (`guest.js`) now both take the avatar through one shared multer **memory-storage** middleware `photos.uploadAvatar` (field `avatar`, reusing the task path's `MAX_UPLOAD_BYTES` + `fileFilter`), so `guest.js` no longer writes a temp file only to `fs.readFileSync` it back and `fs.unlinkSync` it, and `auth.js` drops its own local `multer` instance + import. `me-edit.ejs`'s avatar input changes field `photo`→`avatar` with `id`/`name`/`for` all aligned to `avatar` (matching `onboard.ejs`); the shared `upload.js` preview binds `#photo || #avatar` so both the task-photo form (`task.ejs`, untouched) and the profile-edit avatar form still preview. New `tests/avatar-intake.test.js` drives the real routes — onboarding a valid JPEG sets `avatar_path` to an on-disk `.jpg` (AC2), and a profile-edit replacement removes the old file and repoints `avatar_path` (AC3), both inversion-sensitive. Wave 2 / Lane E (brand+onboarding), footprint disjoint from Lane F. Goal A (feels finished; one avatar path to maintain). Routine code: issue-review round-1 FAIL (stale/out-of-range Background `file:line` citations) → corrected → fresh reviewer-issue PASS; bias-gate PASS before fan-out; PR round-1 two independent `reviewer-pr` PASS, `reviewer-design-philosophy` FAIL (information-leakage red flag: `me-edit` `id="photo"`/`name="avatar"` mismatch leaning on a hidden `upload.js` coupling) → fixed (id/name/for unified + `upload.js` dual-selector); round-2 fresh `reviewer-pr` PASS + design-philosophy FAIL again (a now-false comment claiming avatar memory storage is configured "in auth.js") → comment corrected + swept; round-3 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS (0 blocking; minors/nits only). Rebased onto #86 (`b3f271b`, disjoint code) with a fresh `reviewer-pr` re-confirmation (306 tests green on the integrated tree) before merge. Follow-up (out of scope, pre-existing): `/onboard` lets a multer avatar error reach the generic 500 handler while `/me/edit` shows a friendly flash — captured for unification. Merge-on-green.
- 960759d — #87 — feat(feed): comments on photos with host moderation — a `comments` table (per-photo, both FKs `ON DELETE CASCADE`, its own `taken_down` moderation flag, guarded `IF NOT EXISTS`), a guest-only `POST /p/:submissionId/comments` (per-route `requireGuest`; trims and rejects empty or >300-char bodies server-side; missing/taken-down 404s via `feed.detail`), a flat escaped comment list under each feed item (`<%= %>` — never `<%- %>`, no auto-linking, NO nested replies, NO notifications), and host moderation: `GET /admin/comments` + `POST /admin/comments/:id/hide|restore` on a new `admin-comments.ejs`, plus a "Comments" admin-nav link so hosts can find it. Comment enrichment (`attachComments`) mirrors #86's `attachLikeCounts` — keyed off the already-visible ids `feed.allVisible()` returned, filtering only the comment-level `taken_down = 0`, so `src/services/feed.js` stays the sole owner of submission visibility; hidden comments and comments on taken-down photos never surface. `COMMENT_MAX_LENGTH = 300` is a single source feeding both the server check and the input's `maxlength`. New `tests/photo-comments.test.js` covers AC2–AC8 (incl. the XSS-escaping and 300-char-cap guards) with real-value, inversion-sensitive assertions. Wave 2 / Lane F. Goal D (a shared record people engage with) + Goal C (hosts stay in control). High-stakes code (XSS + moderation): issue-review PASS; PR round-1 three independent Opus `reviewer-pr` unanimous PASS; `reviewer-design-philosophy` FAIL twice — round 1 on a duplicated `300` literal (server constant vs a hard-coded `maxlength`) plus a verb-naming query, round 2 on a broader comments-service demand that a fresh, sibling-calibrated round-3 reviewer retracted as an over-flag (comments correctly mirror the inline #86-likes pattern and the `GET /admin/photos` inline-SQL precedent, not held to a stricter bar) — fixed (single-source cap threaded to the view; hide-vs-takedown verb distinction documented; `attachComments` aligned to `attachLikeCounts`); round-3 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS on the final tree. 311 tests green. Depends on #86. Merge-on-green.
- bc53b92 — #86 — feat(feed): likes on photos — a `likes` table (one like per guest per photo, `UNIQUE (submission_id, guest_id)`, both FKs `ON DELETE CASCADE`, guarded `IF NOT EXISTS`), a guest-only toggle route `POST /p/:submissionId/like` (per-route `requireGuest`, so anonymous 403s with no row written; missing/taken-down 404s via `feed.detail`; toggle = delete-if-present else `INSERT OR IGNORE`; redirect to `/feed#photo-<id>`), and a heart control + like-count in the feed action bar (form only when signed in, count always). Like counts are attached in the `/feed` route handler over the ids `feed.allVisible()` already returned — never re-typing the `taken_down` rule — so `src/services/feed.js` stays the sole owner of visibility. New `tests/photo-likes.test.js` covers AC2–AC6 with real DB-row and rendered-count assertions. Wave 2 / Lane F (feed-social), footprint disjoint from Lane E. Goal B (instant reward + status) + Goal D (a shared record people engage with). Routine code: issue-review PASS; PR round-1 two independent Opus `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL (information-leakage red flag — the like-count query re-typed `s.taken_down = 0`, duplicating feed.js's `VISIBLE_WHERE`) → fixed (count keyed only off already-visible ids; redundant `guest` pass-through dropped; comment corrected); round-2 fresh `reviewer-pr` PASS + fresh `reviewer-design-philosophy` PASS on the corrected tree. 304 tests green. Depends on #84. Merge-on-green.
- decb3ee — #171 — feat(loadtest): peak/load test harness for Goal A. New `scripts/loadtest.js` — a dependency-light driver (Node built-in `fetch`, no new package) that runs N concurrent virtual guests against a live event-seeded instance over the real hot paths — sign-in `/j/`, `/`, `/tasks`, `/gallery`, `/leaderboard`, and the heavy `POST /tasks/:id/submit` (multer + sharp + synchronous better-sqlite3). It **discovers an active task id at runtime** (parses the real `/tasks` markup — a hardcoded id could 404 and silently skip the thumbnail+insert) and **round-trips the signed `gsid` cookie** from the server's `Set-Cookie` (synthesizing the token would fail signature verification). Pure helpers `percentile` (nearest-rank) / `summarize` (p50/p95/p99 + errorRate, `status >= 500` counts as error) / `evaluate` (Goal-A bar: `0` `5xx` and `p95` under target) are unit-tested with exact-value assertions; the CLI (`--base-url`/`--concurrency`/`--duration`|`--requests`) prints a summary and `process.exit`s non-zero on a missed bar. `docs/loadtest.md` documents the procedure, the bar, and a plain caveat that this is a local test and real **venue wifi** is a separate on-site dry-run. Routine code + new component: issue-review + architecture-review PASS; PR round-1 two `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL (three obvious-code violations — comments describing behavior the code did not perform: `timedFetch` "status 0" vs code 599, `pickTaskId` describing HTML-scraping while returning a constant, `captureSignedCookie` claiming a split it did not do) plus a substantive PR finding (hardcoded task id could 404 and skip the heavy path) → fixed (comments corrected to match code; runtime task discovery via `extractActiveTaskIds`/`chooseTaskId`; single 599 sentinel); round-2 fresh `reviewer-pr` + fresh `reviewer-design-philosophy` PASS; round-3 fresh `reviewer-pr` PASS on the final tree after a one-line doc-example fix. 299 tests green. Depends on #166. Merge-on-green.
- e77828f — #169 — docs(test-plan): manual pre-wedding walkthrough checklist. New `docs/test-plan.md` — a human-runnable QA checklist run against the #166 100-guest event seed, organized as Setup + one `##` section per goal (A–D) + Cross-cutting, each scenario written as Steps → **Expected:** → `- [ ]` pass/fail box (36 boxes across 32 scenarios). Covers QR sign-in / `/onboard` / session / maintenance (A); submit → points → auto badges 5/10/15 + `/leaderboard` + special badges (B); task CRUD/reorder + points ± + badge award/remove + `takedown`/`restore` + guest admin + QR sheet (C); `/gallery` / `/feed` / photo-detail / profiles + `/admin/export` ZIP + `summary.xlsx`, with taken-down photos "excluded from the gallery but included in the export" (D); and cross-cutting `404` on a taken-down `/uploads/` URL, upload limits, admin lockout, 404/500 pages. Linked from the README Documentation list. Doc-only: issue-review round-1 FAIL on the #115 header-number naming rule → fixed → fresh reviewer-issue PASS; PR `reviewer-pr` PASS with all six structural ACs verified by count. Depends on #166. Merge-on-green.
- 70f3b63 — #166 — feat(seed): realistic ~100-guest event seed for pre-wedding testing. New `tests/helpers/event-fixture.js` (`seedEvent(db,{guests,seed})`) generates a deterministic, LCG-seeded event — 20 event-prefixed tasks (GARDEN's 15-task threshold reachable), an engineered point spread (unique top scorer, mid-pack ties, many low/zero), ~40% avatars, varied bonus/social, and moderated (`taken_down`) photos — with badges written only through `scoring.recomputeAutoBadges`/`awardSpecialBadge` (never hand-inserted) and conforming filenames matching photos.js's `ORIGINAL_RE`/`THUMB_RE`. New `scripts/seed-event.js` installs real photos on disk (round-robin `fixtures/sample-photos`), takes `--guests`/`--seed`/`--force`, validates the guest count, and refuses a non-event DB with a "refusing to clobber" guard before any delete. `tests/event-fixture.test.js` asserts AC1–AC10 with real-value checks (278 tests green). Unblocks the pre-wedding manual walkthrough + load test. Routine code: bias-gate PASS before fan-out; PR round-1 3-Opus panel — 2 PASS, 1 FAIL (a genuine AC4 defect: at small `--guests`, a zero-completion flagged guest was skipped so `taken_down` could be 0, contradicting the code's own floor comment) → fixed (takedown row written for every flagged guest, manifest sized exactly, `--guests` validation, cross-process determinism test); round-2 fresh `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the corrected tree; issue-review + architecture-review PASS. Merge-on-green.
- 1c8936e — #115 — process(issues): GitHub number = issue identity — create the issue on GitHub first, name the draft by the returned number. Reconciled the issue-creation flow across the four governing files onto **create-first**: `.claude/commands/build.md` step 2 and `agents/orchestrator.md` step 1 now `gh issue create --label needs-issue-review` FIRST, capture the assigned number `N`, then write the draft `data/wip-issues/<N>-slug.md` — retiring the locally-minted `NNNN` that let two parallel sessions collide (the 0057–0061 dupes, 2026-07-02). `skills/issue-create.md` names the draft by the GitHub number, corrects its stale `issues/*.md` path to `data/wip-issues/`, and points at a new `standards/issue-standards.md` "Naming" rule (file `N` = `# N —` header = `(#N)` = the GitHub issue number). Remediated the **gitignored** `data/wip-issues/` locally (not committed): deleted the nine 0057–0061 drafts whose GitHub issues are merged/closed, renamed the live archiver draft to `100-*`, and renamed this issue's own draft `0069` → `115` — no two drafts share an `NNNN` prefix. System-level (`.claude/` + `agents/` + `skills/` + `standards/`): issue-review round-1 FAIL (reviewer-architecture caught the premise mislocating the draft-first ordering — it lives in `build.md` step 2, not `orchestrator.md` — and omitting `build.md` from scope) → rewrote the issue to reconcile all four files + correct the premise + add `build.md` to scope; round-2 reviewer-issue + reviewer-architecture PASS. bias-gate PASS; PR two independent reviewer-pr PASS + reviewer-design-philosophy PASS on the staged tree (accepted minor: `issue-create.md` points to the standard for the `# N —` header rule rather than inlining it). Merge-on-green.
- 0ee5b9e — #140 — process(governance): light governance for the epic — drift-check + status-vs-intent boundary. Extended `agents/reviewer-tracker-sync.md` to reconcile the epic **#126** checklist against live issue states — two **advisory** (`minor`/`nit`) findings that never force a FAIL on their own: **checkbox-vs-state drift** (an epic item unchecked while its issue is CLOSED, or checked while OPEN) and a **dangling epic reference** (an epic item citing an issue number absent from the board list), each resolved against the in-scope `gh issue list` read (no new per-issue read) and cited by issue number; the agent stays read-only (never ticks or edits the epic/board). Added a `DESIGN.md` decision "Planning governance: agents tick status, the owner reshapes intent (#140)": agents may update **status** (tick a box on merge); **intent** changes (reorder / rescope / gate) are **surfaced to the owner** and **never silent** — stated explicitly as **advisory** surfacing, not a merge- or build-blocking gate, so it does not reinstate the #150 owner-merge boundary. System-level (`agents/` + `DESIGN.md`): issue-review round-1 PASS with two minors surfaced (AC3 presumed a milestone-grouping invariant #139 does not define; AC4 could read as reinstating the retired owner gate) → rewrote AC3 to a well-defined dangling-reference check + AC4 to require the advisory-not-gate clause, round-2 reviewer-issue + reviewer-architecture PASS; bias-gate PASS before each fan-out; PR round-1 two reviewer-pr PASS + design-philosophy **FAIL** (internal contradiction — the dangling-ref check specified a `gh issue view <N>` read the Input scope forbade; information-leakage from duplicated `gh` command strings; an overlong Role restating the read-only negations) → fixed (resolve against the in-scope board list; name each read once and reference it; Role reduced to a single-responsibility statement with negations consolidated in `## Read-only`; drift findings pinned to advisory `minor`/`nit` severity), round-2 two independent reviewer-pr PASS + fresh design-philosophy PASS on the final tree. Merge-on-green.
- 1200a17 — #120 — refactor(views): one shared message-card partial; stop copying the brand card five times. New `src/views/partials/message-card.ejs` assembles the themed access-gate/error card (heart + heading + body) once, rendered from named locals (`title`/`heading`/`paragraphs`/`links`/`hint`), reusing `head.ejs` (fonts + theme.css + title) and the `heart.ejs` ornament; the card CSS moves into `theme.css` as a new `.message-card` component built from the design tokens (distinct from the existing `.card`). The five hand-duplicated brand cards are retired: `unknownLinkPage()` (auth.js, 404) and `linkRequiredPage()` (session.js, 403) now `res.render` the partial, and `404.ejs`/`error.ejs`/`maintenance.ejs` each become a one-line include — the heart SVG path now lives in exactly one place (`heart.ejs`, AC3). The partial never reads `res.locals.guest` (safe from middleware + for signed-out visitors), and the user-controlled 404 `url` stays HTML-escaped inside a `<code>` element (proven by a direct-render `<script>` escaping test). 237 tests green at review (255 on the merged tree). Wave 1 / Lane B; the theme.css hold (Lane C's #81 + #84) was released before merge — the new `.message-card` section was a clean append, manually inspected for dup/conflicting rules against the merged theme.css. Reviewed: issue-review PASS; PR round-1 panel — two Opus `reviewer-pr` PASS, one FAIL that was an invalidated reviewer error (ran `npx jest` instead of the project's `npm test`/vitest, producing spurious `vi is not defined` failures; actual `npm test` green, confirmed by the two other reviewers + a re-run); round-2 fresh `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the staged tree, 0 blocking. Merged code-only (BUILDLOG deferred to this entry) to clear the concurrent 4-lane BUILDLOG merge churn. Follow-ups (out of scope, pre-existing): 404 renders `[object Object]` when the `url` local is omitted (EJS `with(locals)` scoping) on the guest.js/community.js 404 call sites; heart color set via an inline style rather than a theme.css rule. Merge-on-green.
- e673bc0 — #124 — feat(reliability): during-event backup + restore of data/. (Entry deferred from #124's own PR — that PR merged code-only to break the same BUILDLOG churn.) New `scripts/backup.js` writes a consistent, WAL-safe snapshot of the event's irreplaceable `data/` — the SQLite DB via better-sqlite3's online `.backup()` API (not a torn `fs.copyFile`) plus recursive copies of `uploads/` and `thumbs/` — into a timestamped `backups/<ts>/` under a new `BACKUP_DIR` config key (default `<ROOT>/backups`, a sibling of `data/`, deliberately outside it). The backup tree is gitignored so the DB and guest photos can never be committed — AC6 verified via `git check-ignore` (the architecture review caught that the plan as first written would otherwise land irreplaceable event data in git). README documents run + restore (with the create-`data/`-first step) and the teardown-clears-both note; `tests/backup.test.js` seeds 8 guests / 25 submissions / 4 guest_badges / 6 uploads / 6 thumbs, backs up, wipes `data/`, restores via the documented steps, and asserts exact counts + a consistent (not torn) snapshot. Event-critical reliability (Goal D), Wave 1 / Lane B. Reviewed: issue-review + architecture-review PASS (arch FAIL round 1 on the gitignore gap → issue revised → fresh arch PASS); PR round-1 three-Opus panel FAIL (documented README restore broke when `data/` absent) → fixed; round-2 design-philosophy FAIL (backupData four-arg vs config) → documented as the intentional override-respecting seam; round-3 fresh reviewer-pr PASS + design-philosophy PASS on the final tree; rebased-tree fresh reviewer-pr PASS. Follow-ups (out of scope): README restore brittle if the empty-`data/` precondition is skipped (nests `data/uploads/uploads`); restore assumes `thumbs/` present in the snapshot. Merge-on-green.
- 09826db — #83 — feat(gallery): search + default sort-by-points in the by-person / by-task grouped views — the `?view=task` / `?view=user` views gain a plain `GET` search box (`?q=`), server-filtered: groups whose heading (task title / guest name) does not contain the query, case-insensitively, are dropped. And the by-person view now defaults to most-points-first — guest groups ordered by total points descending, tie-broken by name — so the people who have shared the most rise to the top. `feed.grouped(kind, taskFilter, q)` gains the optional trimmed `q` (blank/absent keeps the old 2-arg behavior, so existing callers and `feed.test.js` are untouched) and, for the user view, attaches each group's `guest_id` + a precomputed `points` field (`scoring.getPoints`, the single scoring authority #104 — reused not re-derived, computed once per group) and sorts on the named `points` field. An empty result renders the existing "No photos yet" empty state (HTTP 200). New `tests/gallery-search.test.js` covers form presence in both views, name/title filtering, points-first order, blank-`q` parity, the no-match empty state, and a taken-down-only guest producing no heading. Goal C/D (find people + moments) + Goal B (points pull engagement). Routine code: bias-gate before fan-out; round-1 two independent Opus `reviewer-pr` PASS but `reviewer-design-philosophy` FAIL on one information-leakage major (the comparator reached through `photos[0].guest_id`) → fixed (named `guest_id`/`points` group fields, `getPoints` once per group); round-2 fresh `reviewer-pr` PASS + fresh `reviewer-design-philosophy` PASS on the corrected tree; issue-review PASS. Merge-on-green.
- 76ee0dc — #82 — chore(test): realistic demo/test fixtures + CC0 sample photos. New `tests/helpers/demo-fixture.js` exports `seedDemo(db)` — a clean-then-seed (delete-then-insert in one transaction, scoped to a `Demo:` task prefix so real tasks/badges are untouched) that builds 10 named guests (≥2 avatars, varied `bonus_points`) + 6 demo tasks + exactly 18 submissions (16 visible + 2 taken-down) engineered under the app's scoring rule (visible count + bonus) so the leaderboard has one unique top (Ava=6) and a mid-pack tie (Liam & Priya=3), spread across ≥4 tasks and ≥6 guests; a deterministic `MANIFEST` gives every submission a storage-serve-conforming `photo_path`/`thumb_path` (matching `photos.js` `ORIGINAL_RE`/`THUMB_RE`, so the demo gallery actually loads). `scripts/seed-demo.js` installs the bundled samples under those conforming names + real `sharp` thumbnails and asserts at install time that `photos.makeThumb`'s returned name still matches the manifest (fail-fast on any future thumb-naming drift). 9 bundled CC0 / public-domain sample images (original `sharp`-generated artwork — in-license, no external/paid API) + `SOURCES.md`. `tests/demo-fixture.test.js` verifies AC1 (tie/top via the app's own `scoring.leaderboard()`), AC2 (taken-down thumbs never leak into `GET /gallery` while a visible one shows), AC3 (every submission's files on disk), AC5 (named export), AC6 (idempotent exact counts 10/18). Routine test/fixture-only addition (new files under `tests/`, `scripts/`, `fixtures/`; reads app source, edits none). Issue tightened during issue-review to specify the file-install seam + conforming filenames + generated-CC0 sourcing. Reviewed over 4 rounds: issue-review PASS; PR round-1 two-Opus panel unanimous PASS (avatar-dedup + derived-count nits fixed); design-philosophy FAILed twice — first on a hand-rolled scoring formula duplicating `scoring.js` (→ read `scoring.leaderboard()`), then on the thumb-name rule + duplicated insert loops (→ single `thumbNameFor` helper + install-time drift assertion + a shared `insertOne` helper) — then round-4 fresh Opus PR PASS + fresh design-philosophy PASS (0 defects). Rebased onto sibling merges (#125, then #81/#84/#141) with a fresh reviewer-pr re-confirmation (full suite green against the changed gallery/feed) before merge. Merge-on-green.
- ee35871 — #84 — feat(gallery): full-screen photo feed — `GET /feed` renders every visible photo as a full-screen vertical scroll, newest-first, each at full resolution (`/uploads/<photo_path>`) with its author (linked to `/u/<guest_id>`) and caption. The gallery grid's thumbnails now link to `/feed#photo-<id>`, so native fragment anchoring drops the guest straight onto the photo they tapped — no JS; `scroll-margin-top` on `.feed-item` clears the sticky header, desktop caps the photo at 560px. `feed.js` gains `allVisible()` — the flat, unpaginated, newest-first query — so every `#photo-<id>` resolves (`recentPage` paginates, `grouped` returns sections; neither guarantees it). Each item leaves an empty `.feed-actionbar` placeholder for the future like/comment/points controls (separate issues; none ship here). `/p/<id>` stays as the permalink (still 200/404); `public-profile.ejs` keeps its `/p/<id>` link (out of scope). The three tests that asserted the gallery links to `/p/<id>` — `gallery.test.js` AC1, `gallery-grid.test.js` AC4, `photo-detail.test.js` AC5 — are repointed to `/feed#photo-<id>`; new `tests/photo-feed.test.js` covers ordering, anchors, author/caption, the gallery link, taken-down exclusion, and the permalink. Precondition for Wave 2 (likes/comments/points hang off `.feed-actionbar`). Goal D — the shared record seen as it fills. Routine code: bias-gate before fan-out; round-1 two independent Opus `reviewer-pr` PASS (unanimous), a stale test comment fixed, then a fresh `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the corrected tree; issue-review PASS. Merge-on-green.
- 749fe10 — #141 — feat(tooling): `/session` skill — render a paste-able session brief from the board on demand. New `skills/session-brief.md` documents the `/session <track>` procedure: read the epic #126 board via `gh issue view 126` and each listed issue via `gh issue view <n>` (no stored copy), then render a paste-able brief listing the track's issues in **build order**, their shared-file collisions (`Touches`), their dependencies (`Depends on`), the `parallel-safe` rule (two file-disjoint, dependency-free tracks are parallel-safe), and the **merge policy by reference** to `DESIGN.md` § "Merge policy" and `CLAUDE.md` — no second source of truth. Depends on #139 (the board structure it renders now exists). System-level (`skills/`): issue-review round-1 FAIL (four majors — ACs described runtime behavior not checkable against the skill file; plan step 2 named no referenceable artifact; `Depends on` was prose not an issue number; the artifact was left un-pinned between `skills/` and `.claude/commands/`), rewritten to file-structural ACs + pinned to `skills/session-brief.md` + `Depends on: #139`, round-2 both reviewer-issue + reviewer-architecture PASS; bias-gate PASS before each fan-out; PR review two independent reviewer-pr PASS + reviewer-design-philosophy PASS (0 blocking; description-folded-block and unverified-anchor nits, the latter independently confirmed valid at `DESIGN.md:61`) on the staged tree. Rebased onto sibling merges (#118/#125, then #81) and re-formatted with Prettier for CI (`prettier --check .` covers `.md`); each rebase and the reformat was re-reviewed fresh — two independent reviewer-pr PASS on the final tree. Merge-on-green.
- 3b5b84c — #81 — feat(gallery): Instagram-style photo-wall grid — the /gallery grid is now an edge-to-edge photo wall: a gapless 2-column square grid, full-bleed on phones (`width: 100vw` + `margin-inline: calc(50% - 50vw)`) and capped to a centred 3-column grid at a single `@media (min-width: 700px)` breakpoint. The old `@media (min-width: 620px)` `.gallery-grid` override was folded into that one desktop breakpoint (its unrelated `.badge-grid` / `.qr-sheet` / `.site-header-inner` rules stay put), so exactly one media query sets the grid columns. Per-thumbnail captions (task / "by <guest>" / caption) are removed — the grid shows photos only; the uncropped photo and caption live one tap away at `/p/<id>`, which each tile still links to (`loading="lazy"` kept). `.gallery-item` keeps `aspect-ratio: 1 / 1` so tiles render as real squares — a browser render check caught that dropping it collapses the absolutely-positioned tiles to **zero height** (measured 107×0 → 107×107 with the fix), a regression the string-based tests could not see. The `.gallery-caption` / `.gallery-task` / `.gallery-text` rules stay because `public-profile.ejs` still renders them on its own photo grid. New `tests/gallery-grid.test.js` guards caption-free grids (recent / by-task / by-person), `/p/<id>` links, and taken-down exclusion. Goal D — the shared record as a wall worth scrolling. Routine code: bias-gate before fan-out; round-1 two independent Opus `reviewer-pr` PASS (unanimous) + `reviewer-design-philosophy` PASS on the staged tree; issue-review PASS. Merge-on-green.
- 2ce4160 — #125 — test(e2e): guest happy-path end-to-end + concurrency smoke. New `tests/e2e-guest-happy-path.test.js` drives the real guest journey against the assembled app (scan `/j/:token` → `POST /onboard` → submit a photo → `GET /` shows `1 point` + the completed task title; a fifth distinct submission crosses the BLOOM threshold so the badge appears — asserted ABSENT after 4 and PRESENT after 5 on both the rendered page and the `guest_badges` row) plus a 20-guest concurrency smoke: 20 distinct guests each `POST` one photo to one task via `Promise.all` (handlers interleave at the async `sharp` thumbnail around serialized better-sqlite3 writes), asserting every response `< 500`, the task-scoped visible-submission count `== 20`, and 20 distinct, correctly-attributed `guest_id`s — no lost or mis-attributed write. AC3 was tightened during issue-review: the original "SQLite-under-concurrency / no SQLITE_BUSY" framing overclaimed (the app runs a single synchronous connection, so in-process writes serialize and cannot raise SQLITE_BUSY), reframed to what the smoke actually proves. Routine test-only addition (reads existing routes/config/schema, edits no app source). Reviewed: issue-review PASS (round 2, after the AC3 reframe); PR round-1 two-Opus panel unanimous PASS; design-philosophy round-1 FAIL on two red flags (vague-name empty-string sentinel; information-leakage hand-copied title prefix) → fixed (named `PRE_ONBOARD_NAME` constant; assert the captured `title` value); round-2 fresh Opus PR PASS + fresh design-philosophy PASS (0 defects) on the final tree; rebased onto #118+#139 with a fresh reviewer-pr re-confirmation (206 tests green) before merge. Merge-on-green.
- 0950fc4 — #139 — process(roadmap): board-derived, session-structured planning — retire the stored `docs/roadmap.md`. Records a `DESIGN.md` decision that the build roadmap is **derived from the board on demand** (epic #126 + milestones + each issue's `Depends on`/`Touches`), not a committed file, and that sessions are **grouped by file-locality**, not theme; names `PLAN.md`/`CONTEXT.md` as the out-of-scope historical refactor plan (a distinct decision from "Scoring derived, not stored"). Restructured epic #126 into 19 file-local sessions of ≤4 issues, each carrying a `Files:` annotation and exactly one relation tag (`depends on` / `parallel-safe with` / `parallel after #<root>`), with #118/#120/#81 as single-member fan-out roots, and removed the dangling `docs/roadmap.md` references (`git ls-files docs/roadmap.md` was already empty — the file was untracked). Doc-only (`DESIGN.md`) + system-level: issue-review PASS (reviewer-issue + reviewer-architecture); bias-gate PASS before each fan-out (the issue-stage gate caught and stripped a planted "confirm no component duplication" suspicion from the architecture briefing); two independent reviewer-pr PASS on the staged tree — design-philosophy gate N/A (doc-only, not an implementation artifact). One accepted minor, no change required: AC3 enumerates two tag forms while AC5 mandates a third (`parallel after #<root>`); the artifact uses all three per AC5, so AC3's wording is under-inclusive on the issue side, not an implementation defect. Merge-on-green.
- c935340 — #118 — refactor(config): one source of truth for upload/badge constants. Deleted the dead/drifted `config.js` copies of `MAX_UPLOAD_BYTES` (12 MB, contradicting photos.js's live 15 MB), `THUMB_WIDTH`, `ALLOWED_MIME`, and `BADGE_THRESHOLDS` plus their four lowercase aliases, and the false comment claiming scoring.js imports the thresholds from config; `auth.js`'s avatar multer now reads `photos.MAX_UPLOAD_BYTES`, so avatar and task-submission uploads enforce one shared limit. Repointed `tests/smoke.test.js` to `scoring.BADGE_THRESHOLDS` (the real owner) and added `tests/avatar-upload-limit.test.js` — a behavioral supertest that posts a real ~15 MB JPEG just under and a buffer just over the shared limit, failing if auth reverts to a separate/hard-coded ceiling. 203 tests green. Routine refactor (Wave 1 / Lane B). Reviewed: issue-review PASS; PR round-1 three-Opus panel unanimous PASS + design-philosophy PASS on the staged tree, 0 blocking/major (nits only: the over-limit test can't distinguish a smaller limit on its own — the under-limit test carries the inversion pin; a now-slightly-stale "any stray lowercase reference" comment header on the alias block). Merge-on-green.
- d36efce — #150 — process(pipeline): retire the owner-merge boundary — the orchestrator now auto-merges every PR that passes AI review + green CI, for every change type including visual / product-direction. The owner does not perform merges, so PRs held for a manual merge accumulated open and nothing shipped; `main` branch protection already requires **0 approving** reviews and gates solely on CI, so a human merge click was never GitHub-enforced, only convention. Retired the two-branch policy across the seven governing / operational files that stated it: `.claude/commands/build.md` (step 7), `agents/orchestrator.md` (ship-flow + the Constraints "owner confirms the visual result" / "sanctioned final-eye gate" clause), `CLAUDE.md` (step 5), `.claude/commands/resume.md`, `docs/RESUME-STATE.md`, and `WHAT-IT-CHECKS.md` (the plain-English owner guide); plus a new `DESIGN.md` "Merge policy: owner-merge boundary retired" decision (records the rationale — branch protection, 0 approving, revert-over-gate — and that it supersedes the orchestrator.md owner-visual-gate). "No human reads code in the critical path" preserved; only the human pre-merge gate is removed — owner control stays upstream (issue-speccing) and downstream (revert). System-level: issue-review PASS (reviewer-issue + reviewer-architecture); bias-gate run before each fan-out (the round-2 gate caught and required stripping a "governing agent-spec" framing + a missing full-scope clause from the design-philosophy briefing — two independent gate agents disagreed on identical text, resolved by applying the stricter one's edits); round-1 two-Opus PR PASS but design-philosophy FAIL on two real internal contradictions the retirement left behind (RESUME-STATE.md stated the boundary as live in four more places; the DESIGN.md branch-protection section still called the manual merge click "the review gate") → fixed; round-2 two independent Opus PR PASS + fresh design-philosophy PASS (0 blocking; 2 nits) on the final tree. Follow-up nits (out of scope): `DESIGN.md:65` comma-joins `strict = true` with the `required_pull_request_reviews` key though `strict` lives under `required_status_checks`; `WHAT-IT-CHECKS.md:44` closing-line phrasing. Merge-on-green.
- a7678d1 — #148 — infra(agents): wire worktree isolation into /build — enforce it, not just document it. #113 shipped the worktree creator (`tools/new-agent-worktree.ps1`) but nothing the pipeline actually runs invoked it, so concurrent `/build` sessions could still collide in the shared primary checkout. New `tools/assert-worktree.ps1`: a pure, side-effect-free guard (mirrors `check-gate.ps1`) that passes only inside a linked worktree (`git rev-parse --absolute-git-dir` contains `/worktrees/`) and fails closed in the primary checkout with a stderr remediation message naming the creator; writes via `[Console]::Error.WriteLine` (not `Write-Error`) so the success path is silent on stderr. Wired in: `.claude/commands/build.md` gains Step 0 (Isolate) before Research and guards step 6's per-issue `git switch -c` with the same assertion on the same step; `agents/orchestrator.md` carries the precondition as its first operating rule (binds direct, non-`/build` invocations too); `DESIGN.md` records the enforcement, closing the prose-only gap #113 left open. System-level: bias-gate PASS + two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the staged tree; issue-review (reviewer-issue + reviewer-architecture) PASS. One inconsequential nit (documented `powershell -File` invocation vs the `-ExecutionPolicy Bypass` form used elsewhere — consistent with the rest of build.md, works in the real pipeline context). Merge-on-green.

## 2026-07-02

- #116 — ci(issues): server-side guard flagging unreviewed issue creation. New `.github/workflows/issue-guard.yml` fires on `issues.opened` (least-privilege `permissions: issues: write`, `actions/github-script@v9`): if the opened issue lacks the `needs-issue-review` marker it adds `unverified-issue` and comments naming the required flow; an issue carrying the marker (the sanctioned pre-review birth state) is left alone. `gh issue create` is a direct API call that trips no local hook, so the only enforcement point is server-side — this is the advisory-visibility sibling of #48 (un-bypassable merge enforcement), tamper-evident not tamper-proof. Runs on GitHub-hosted Actions (not the event laptop), consistent with the `commit-gate-integrity`/`merge-association` backstop (#46), so the laptop-only constraint still governs the running app. The `unverified-issue` label was pre-created out-of-band (`gh label create`, #B60205); the guard assumes it, never creates it. Decision recorded in DESIGN.md. Live firing begins once the workflow is on `main` (GitHub runs issues-event workflows from the default branch), so pre-merge verification was by tracing + a local Node simulation of both cases + confirming `github-script@v9` and the `addLabels`/`createComment` methods resolve. System-level, stacked on #114 — reviewed: issue-review PASS (reviewer-issue + reviewer-architecture); bias gate clean before each fan-out (it took three passes to strip mechanism-leak wording — first `pins`, then `github-script` — from the reviewer briefing); round-1 two-Opus PR PASS, design-philosophy FAIL on a genuinely misnamed boolean (`reviewed = labels.includes('needs-issue-review')` asserted the opposite of the truth, since carrying the marker means NOT-yet-reviewed) → renamed to `hasReviewMarker` + a clarifying comment; round-2 two independent Opus PR PASS + fresh design-philosophy PASS (0 blocking) on the final tree. Left as an open PR for the owner to merge (system-level governance). Follow-up (out of scope): the guard reads the `opened` payload's label snapshot; a live `listLabelsForIssue` re-fetch would harden toward tamper-proof, but the bar here is tamper-evident by design.
- #114 — ops(ci): branch protection on main (require branches up to date before merge). New `tools/apply-branch-protection.ps1` PUTs GitHub branch protection to `main`: `required_status_checks.strict=true` (require up-to-date), the five real produced check-run names (`commit-gate-integrity`, `lint`, `test`, `merge-association`, and CodeQL's produced name `Analyze (javascript)`, not the workflow label `CodeQL`), `required_approving_review_count=0` (the solo owner is the merge gate; GitHub forbids self-approval, so ≥1 would lock the owner out), `enforce_admins=true`, `restrictions=null`; idempotent by construction (fixed payload + full-replace PUT) and self-checks the payload before sending. Closes the concurrent-merge race: two PRs green against an older `main` can merge close together and leave `main` in a state CI never ran on the combined tree; `strict` forces a stale branch to update + re-run CI first. Slug resolved dynamically via `gh repo view`. Applied to the live repo and verified: strict + 5 checks live, no phantom checks, and PR #142 stays MERGEABLE (owner not locked out). Decision recorded in DESIGN.md. System-level, stacked on #113 — reviewed: issue-review PASS (reviewer-issue + reviewer-architecture); bias gate clean before each fan-out (it caught and corrected a property-leak in the reviewer briefing); round-1 two-Opus PR PASS, design-philosophy FAIL on a `temporal-decomposition` flag that a fresh design-philosophy reviewer then retracted as an over-application to a linear leaf script (consistent with #113's ruling on the analogous `new-agent-worktree.ps1`); the two genuine minors (dead `$top` guard, uncommented write/read key asymmetry) were fixed; round-2 two independent Opus PR PASS + fresh design-philosophy PASS (0 defects) on the final tree. Left as an open PR for the owner to merge (system-level governance). Follow-ups (out of scope): the write-side `contexts` key carries a GitHub "closing down" notice in favor of `checks`; a comment says `ci.yml` where CodeQL's name lives in `codeql.yml`; `merge-association` doesn't run on push (harmless for the PR-merge path).
- #113 — infra(agents): worktree-per-agent isolation. New `tools/new-agent-worktree.ps1 -Branch <name>` gives each file-mutating agent its own git worktree (own working dir + branch, shared object store) so two concurrent sessions can't stash/revert/switch-branch-under each other — the 2026-07-02 Dependabot-vs-refactor collision that shared one folder. Key property: the commit gate is live in every worktree with zero extra config, because `core.hooksPath=.githooks` is a relative path in shared config and `.githooks/` is a tracked dir, so it resolves to `<worktree>/.githooks`; the script asserts it by running `check-gate.ps1` inside the new worktree and echoes that tool's own output (not a self-authored guarantee), failing clean if the branch is already checked out elsewhere or the target path exists. Decision+rationale in DESIGN.md, operating rule in CLAUDE.md, how-to in README. System-level — reviewed: issue-review PASS (reviewer-issue + reviewer-architecture); bias gate clean before each fan-out; round-1 two-Opus PR PASS but design-philosophy FAIL (info-leakage: the gate-not-live error narrated `core.hooksPath` internals; the success message overstated what was verified), fixed (trimmed the error to fact + one recovery step, success now echoes check-gate's own line, unified the stderr idiom), round-2 two independent Opus PR PASS + fresh design-philosophy PASS (0 defects) on the final tree. Left as an open PR for the owner to merge (system-level governance change). Follow-up: `check-gate.ps1:14` points at a nonexistent `setup.ps1` (should be `setup-hooks.ps1`) — pre-existing, out of scope for #113.
- 62b8c0f — #119 — refactor(bootstrap): remove dead build-order scaffolding. The app was built section-by-section and still carried "not present yet" guards; every file now exists, so those branches were permanently dead. `app.js`: replaced `mountRouterIfPresent()` + per-router `fs.existsSync` guards with direct `require`+`app.use` (auth `/`, admin `/admin`, guest `/`, community `/`) — each require kept adjacent to its mount (matching the session middleware), the admin-before-guest ordering comment on the admin mount, order/paths preserved; dropped the conditional `attachGuest` install and the temporary "server is running" plain-text home route. `auth.js`: hoisted `photos` to a module-scope require and stripped `trySaveAvatar`'s "photos.js not created yet" try/catch + `typeof saveAvatar` guard. 201 tests green. Reviewed: issue-review PASS; PR round-1 two-Opus panel PASS; design-philosophy round-1 FAIL on a stranded ordering comment (the require/mount split left the "admin before guest" note on a bare require) → fixed by keeping require+mount adjacent; round-2 fresh Opus PR + design-philosophy PASS, 0 defects. Follow-up nit: the added AC3 test only asserts the retired placeholder string is absent (redundant — AC3 is positively covered by the pre-existing guest-home test). Merge-on-green.
- ece6eb4 — #106 — refactor(submit): extract a submission-intake module. Moved the whole "submit or replace a photo for a task" sequence out of `POST /tasks/:id/submit` in `guest.js` into new `src/services/submissions.js`. `submitPhoto({ guestId, taskId, file, caption })` — a deep module taking a plain `{ filename, path }` descriptor (no req/res/Express/multer) — owns the active-task check, thumbnail generation, caption normalization (trim + 500-cap), the `UNIQUE(guest_id, task_id)` insert-or-replace upsert with superseded-file cleanup, and a non-fatal badge recompute, returning a status object (`created`/`replaced`/`task_inactive`/`thumb_failed`) the route maps to flash+redirect or a 404. Also closes an orphan-file leak: an inactive-task POST used to 404 AFTER multer wrote the file; `submitPhoto` now deletes it. The route keeps only multer + error/missing-file branches + one call + the status map (no submissions SQL, no direct recompute). New `tests/submission-intake.test.js` (13 tests) drives `submitPhoto` directly for AC1-7 and the route for AC9; guest tokens are per-insert unique (retry-safe) and the replaced-flow test is self-contained. 200 tests green. Reviewed: issue-review PASS; round-1 unanimous PASS (two Opus PR + design-philosophy), then fixes for a false cleanup comment (the catch now logs, satisfying AC2's "logged") + test retry-safety + self-containment; round-2 fresh Opus PR + design-philosophy PASS on the final tree, 0 defects. Follow-up nit (out of scope): eslint config lacks `caughtErrorsIgnorePattern`, so the intentional silent `catch (_err)` at `submissions.js:107` warns — a one-line config fix would silence it codebase-wide. Merge-on-green.
- 32290cd — #105 — refactor(moderation): one atomic writer for takedown/restore. `src/services/photos.js` is now the single writer of `submissions.taken_down` for moderation: `hideSubmission`/`restoreSubmission` flip the flag AND call `scoring.recomputeAutoBadges(guest_id)` inside one `db.transaction` (nested as a SAVEPOINT), returning the affected `guest_id` (or `undefined` if the submission is gone). The `admin.js` takedown/restore routes call `photos.hideSubmission`/`restoreSubmission` and guard on `undefined` — no raw `UPDATE submissions SET taken_down` or separate recompute left in the route, so the flag can no longer be flipped without the recount. Added `require('./scoring')` to photos.js (cycle-free — scoring requires only db). New `tests/submission-lifecycle.test.js` drives the admin routes: 5 visible → BLOOM held, take one down → BLOOM revoked, restore → back, admin-awarded special badge untouched. 187 tests green. Reviewed: issue-review PASS; round-1 unanimous PASS — two Opus PR reviewers + design-philosophy, 0 blocking defects (the implementer built to the design-philosophy bar first-pass; one optional follow-up nit: `admin.js:20-21` route-list header still frames takedown as two steps). Merge-on-green.
- b9abb2c — #107 — refactor(gallery): extract a visible-submissions feed module. Moved the "visible submissions (`taken_down=0`) + newest-first ordering" rule out of `src/routes/community.js` into new `src/services/feed.js`: one visibility predicate (`VISIBLE_WHERE`) + two ordering constants (`ORDER_NEWEST_FIRST` and its ascending mirror `ORDER_OLDEST_FIRST` for the newer-neighbor keyset walk), composed by `recentPage`/`grouped`/`guestPhotos`/`detail`/`neighbors`. The `/gallery`, `/p/:submissionId`, `/u/:guestId` handlers now call `feed.*` and render — no SQL / `taken_down` / submissions `ORDER BY` left in the route. Detail reuses `GALLERY_SELECT_BODY` (one shared column body). New `tests/feed.test.js` (16 tests, no HTTP) covers ordering+pagination, taken-down exclusion across every op, and neighbor skip/not-found. Faithful relocation — no guest-visible change. Reviewed over 4 rounds: issue-review PASS; PR panel PASS r1-3; then a stale-ordering-comment fix, a duplicated-SELECT + false-comment fix, and a `kind`→`shape` rename drove the design-philosophy gate to PASS (r4). Round-4 PR FAILed only on AC5's over-broad literal wording — its regex catches the pre-existing, out-of-scope `ORDER BY gb.created_at` badge query in `loadGuestBadges` — and the severity adjudicator ruled that residual plus four nits all inconsequential and issued EXIT AUTHORIZED. Loop closed via adjudication, not a clean both-gate PASS. Merge-on-green.
- 99cac39 — #104 — refactor(scoring): route all points/completed-count reads through the single authority `src/services/scoring.js` and delete three shadow copies of the `points = visible submissions (taken_down=0) + bonus` rule. `admin.js`: removed the `pointsForGuest`/`completedCount` helpers whose always-false `typeof scoring.pointsForGuest` guard silently ran duplicate SQL (a latent bug — the admin guests page never used the authority) → call `scoring.getPoints`/`getCompletedCount`. `db.js`: removed the uncalled `getCompletedCount` export. `export.js`: removed the drifted hardcoded `AUTO_THRESHOLDS = [5,10,15]` constant + the `completedByGuest` loop (Guests-sheet totals now via scoring). `guest.js`: replaced the local submissions `COUNT(*)` with `scoring.getCompletedCount` (the tasks-active denominator query left untouched). New `tests/scoring-single-authority.test.js` cross-checks the admin page, export sheet, and `scoring.*` on one fixture (1 visible + 1 taken-down + bonus 4 → completed 1, points 5), mutation-tested. Routine refactor. Reviewed: issue-review PASS on file; PR round-1 two-Opus panel PASS, then a stale-comment fix (both round-1 reviewers independently flagged `guest.js:7` still advertising the now-deleted `db.getCompletedCount`), round-2 fresh Opus PASS + design-philosophy PASS on the final tree. Merge-on-green.
- 8054cbd — #94 — process(review): reviewers must verify a cited `file:line` is in range. Adds a "Citations must be in range" rule to `standards/adversarial-review-protocol.md` (open the file and confirm the line ≤ its actual line count before citing; an out-of-range or unverified citation is itself a defect), framed as the reviewer-side half of the citation guarantee with the existing orchestrator-side "The spawner must never" #5 as the other half, and `tools/review-runner.ps1` (#128) named as the mechanical enforcement for the JSON-verdict path; plus a matching self-check line in `agents/reviewer-pr.md`'s checklist. Closes the prose-verdict gap the runner doesn't yet cover (reviewers emit prose, not JSON). Surfaced by #47's rounds, where reviewers cited fabricated line numbers. Docs-only (two `.md`). Graduated from backlog (issue-review PASS: reviewer-issue + reviewer-architecture); bias-gate clean; system-level PR bar — two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS, 0 defects (one wording nit). Merge-on-green.
- 1589966 — #93 — docs(tools): point `persist-review.ps1`'s runner reference at #128, not #46. Its header comment said the program-driven review runner was tracked in issue #46, but #46 is the (closed) commit-msg issue-review gate; the runner is #128 (`tools/review-runner.ps1`). One comment line, behavior-preserving (no executable line touched). Surfaced as a #47 follow-up. Graduated from backlog (issue-review PASS); system-level PR bar — two independent `reviewer-pr` PASS + bias-gate, 0 defects (design-philosophy skipped: comment-only). Merge-on-green.
- 7a48c9c — #128 — infra(review): program-driven review runner. New `tools/review-runner.ps1` (+ `tools/review-verdict.schema.md`, `tests/review-runner.test.js`) is the mechanical front door for a reviewer panel: it reads each reviewer's verdict JSON from `-RunDir`, fail-closed citation-validates every cited `file:line` (`file-not-found`/`out-of-range` + a repo-root path-escape guard), refuses to run if `-TreeOid` ≠ the live staged tree, aggregates per `-Mode` (`both-pass` ≥2 all-PASS / `unanimous` ≥1 all-PASS — the protocol's two reviewer-count bars), and only on a fully clean pass calls the existing `persist-review.ps1` + `review_verdict.ps1` — so a recorded PASS is mechanical and a fabricated citation can't produce one. Reuses the single-writers (no new evidence surface); `DESIGN.md` documents it and the directory-of-JSON design choice. Bounded first version (consumes fixture verdict JSON; reviewer agents _emitting_ it is a later slice). System-level: issue-review PASS (reviewer-issue + reviewer-architecture); bias-gate clean before each fan-out; PR review two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the final tree over 6 rounds — the panel found and drove fixes for real defects: identical `-Mode` branches (information-leakage) → genuine divergence; `powershell` hardcoded → edition-detected `pwsh` for Linux CI; a `line: 0` fail-open (PowerShell `0 -ne ''` coercion) → string-compared guard; a path-escape prefix hole admitting `<root>-evil` siblings → separator-bounded check; nested-`pwsh` spawns timing out Linux CI → a 30s per-test timeout; plus `findings_count`/schema-doc nits. 14 isolated tests green (full suite 160 with #68's retry-selfheal). Rebased onto #68 so the pwsh-spawn-heavy suite has stable CI. Unblocks #94. Non-blocking follow-up (design-debt): the FAIL-wins/count aggregation is re-derived here rather than reusing `verdict-core`'s `Reduce-Verdicts`, and a comment overstates that reuse. Merge-on-green.
- f7f3f9b — #68 — test-infra: add vitest `test.retry: 2` to absorb transient pwsh-spawn flakes. On CI, `pwsh` cold-start contention can push a launcher-spawning test past the default 5s timeout — observed 2026-07-02 as identical commits going red/green by scheduling luck (`enforcement-bias-gate` AC2 timing out), the flake that was blocking the correct, reviewed #128 PR. A bounded global retry self-heals a transient miss while a genuine regression (fails every attempt) still fails. New `tests/retry-selfheal.test.js` proves it isn't inert (module-scoped counter: fails attempt 1, passes on retry; reds the suite under `retry: 0`). Graduated from backlog (issue-review PASS); PR routine panel unanimous — two `reviewer-pr` + `reviewer-design-philosophy`, nits only. Unblocks reliable green CI for the pwsh-spawn-heavy suite. Merge-on-green.
- 26cd587 — #85 — docs(scope): admit the social layer (likes, comments, per-photo points) into the north-star + reconcile `DESIGN.md`. Moves the social-layer terms from "not built yet / out of scope" into an admitted, scoped part of Goal D — `docs/north-star.md` now carries the "look up, not look down" framing (lines 54-60). Docs-only, but `docs/north-star.md` + `DESIGN.md` are governing artifacts, so it cleared the system-level review bar. Merged via PR #129. (BUILDLOG line backfilled 2026-07-02 — the original merge omitted it; the gap was caught by the #104 board-sync check.)
- cbddd1e — #117 — process(review): reviewers are read-only, no state-mutating git. Added an explicit `## Read-only` clause + the forbidden set (`git add`/`reset`/`restore`/`checkout`/`stash`/`commit`/`rm` + no file edits) to all ten reviewing agent specs (nine `reviewer-*.md` + `severity-adjudicator.md`); a `## Reviewers are read-only` rule to `standards/adversarial-review-protocol.md` carrying the 2026-07-02 `git restore` incident as rationale; and `## No mutation authority` (AC3) + `## Re-verify the tree oid before recording a verdict` (AC4) directives to `skills/spawn-adversarial-review.md`. The prose binds a reviewer even when instantiated with a broader tool set, closing the hole where a reviewer mutates the exact staged tree its verdict is bound to; `reviewer-tracker-sync` additionally forbids board mutation (`gh issue close`/`edit`) given its Bash/gh access. System-level change: issue-review PASS (reviewer-issue + reviewer-architecture, recorded earlier); the bias gate ran twice before fan-out and caught a real intent-leak — my briefings recited the forbidden-command list, which is the diff's own AC payload — fixed both times, then clean; PR review two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the exact staged tree, single round, 0 defects (one nit). Follow-up nit (no action for this issue): the ~60-word read-only block is duplicated across 11–12 files — per-file prose is the intended design, but a drift-guard test (cf. `tests/classify-dep-pr.test.js`) would catch silent divergence. Correctness/process fix — merge-on-green.

## 2026-07-01

- #66 — bump dev-deps group (eslint 9→10, @eslint/js 9→10, eslint-config-prettier 9→10, prettier 3.9.1→3.9.4): auto-tier dev bump that arrived CI-red because eslint 10 added `no-useless-assignment` to `eslint:recommended`, flagging three dead-store initializers. Fixed first in #110, then #66 rebased onto that → `lint` green → merged. Dev-only; app unaffected (test 145/145).
- #109 (PR #110) — fix(lint): drop three dead-store initializers (`let ok`/`social`/`socialText` → bare `let`) in `src/routes/community.js:67`, `src/routes/guest.js:347`, `src/services/export.js:143`, flagged by eslint 10 `no-useless-assignment`. Behavior-preserving — every `try`/`catch` branch assigns before first read (traced by both reviewers). Issue-review PASS; PR 2-Opus panel PASS + design-philosophy PASS; committed through the local commit gate (3 recorded reviewer verdicts + issue-review). Unblocks #66.
- #97 — bump multer 1.4.5-lts.1 → 2.2.0 (Dependabot PR #11, wedding-critical, `review` tier, security-maintained line): only 2.0 breaking change is the Node floor (repo is Node ≥20). Both upload paths audited; size-limit rejection (`LIMIT_FILE_SIZE`→302) and disk-storage filename shape verified. Issue-review PASS (4 rounds — AC accuracy tightened); PR round-1 2-Opus panel FAIL (an AC over-claimed an existing size-limit test — the gate working), reframed to CI-observed + source-trace, round-2 fresh Opus PASS. No source change. Merge-on-green.
- #98 — bump bcryptjs 2.4.3 → 3.0.3 (Dependabot PR #13, wedding-critical, auth, `review` tier): the dominant risk — an old `$2a$` hash failing to verify — was disproven by regenerating a real 2.4.3 hash and confirming `await bcrypt.compare` returns `true` under 3.0.3 on the live login path (and `false` for a wrong password). 3.x default `$2b$` prefix already tolerated by the pw-strength test. Issue-review PASS (2 rounds — first draft cited sync `compareSync`; ground truth is async `compare` at `auth.js:180`); PR review unanimous 3-of-3 Opus (high-stakes: auth). No source change. Merge-on-green.
- #101 — bump ejs 3.1.10 → 6.0.1 (Dependabot PR #9, wedding-critical, all views, `review` tier): the largest jump (3 majors). ejs 6's CJS `require('ejs')`/`__express`/`include()` surface is unchanged (v5 removed only the unused `client` option; v6 changed only the ESM build). Verified beyond CI (archiver lesson): all 21 templates + 13 live routes rendered under 6.0.1, nested `include()` resolves. Issue-review PASS; PR round-1 split (1 PASS, 1 FAIL on transient pending CI), round-2 fresh Opus PASS on settled green CI. No template edit needed. Merge-on-green.
- #103 (PR #67) — express 4.21.2 → 5.2.1: **DEFERRED, not merged.** A whole-framework major (path-to-regexp route syntax, request/response method changes) with real route-breakage risk and zero guest-facing benefit; Express 4.x is still maintained, so no forcing function before the Aug 7 event. PR closed; migration tracked in backlog #103 (graduate 2026-08-08, gated on a per-route verification pass). Owner-boundary risk call, decided from the North Star.
- #65 — bump the actions-all group with 4 updates (Dependabot PR #65, `auto` tier): `actions/checkout` v4→v7, `actions/setup-node` v4→v6, `actions/upload-artifact` v4→v7, `github/codeql-action` v3→v4 across `ci.yml`/`codeql.yml`. CI-tooling only, cannot affect the running app; CI green, MERGEABLE. Merged on green per the auto-tier policy — no tracked decision required.
- 82c2d76 — #99 — bump sharp 0.33.5 → 0.35.2 (Dependabot PR #14, wedding-critical, `review` tier): thumbnail/avatar image processing, no source change. sharp 0.34/0.35 breaking changes (Node floor →20.9, libvips 8.18.3, install-script model, `failOnError`/`jp2k` removals) don't touch the methods we use (`sharp()`/`.rotate()`/`.resize()`/`.jpeg()`/`.toFile()`). Verified in a worktree: prebuilt `@img/sharp-win32-x64` binary installs with no build tools, `require('sharp')` loads, full suite green, and `makeThumb` yields a 400px JPEG from an 800px source + no upscaling from a 200px source. Issue-review PASS; PR review 2-Opus panel unanimous PASS. Merge-on-green. Follow-up nit: stale `sharp@0.33.5` comment at `photos.js:35`.
- #47/#61 batch note — Dependabot triage: the June-28 batch (#6/#9/#10/#11/#13/#14) was all ~56 commits behind `main`; each is updated onto current `main` so CI exercises the real tree before merge. Verification is behavioral, not CI-trusting: it caught that **archiver 8.0.0 (PR #6) is a breaking pure-ESM rewrite** removing the callable `archiver('zip',…)` factory that `export.js:240` depends on — CI was green only because no test exercises `streamExportZip`. #6 closed (kept archiver 7); migration + the missing export-zip test tracked in #102 (graduate 2026-08-08).
- 9808836 — #96 — bump better-sqlite3 12.2.0 → 12.11.1 (Dependabot PR #10, wedding-critical, `review` tier): same-major minor bump, no source change. The whole June-28 Dependabot batch was ~56 commits behind `main`, so their original green CI tested a 1-suite tree — branch updated onto current `main` and CI re-run green on the full Vitest suite before merge. API audit: only the stable core surface (`new Database`/`.pragma`/`.exec`/`.prepare().get/.run/.all`/`.transaction`) is used, unchanged across v12.3.0..v12.11.1. Issue-review PASS (2 rounds — a first amendment cited a nonexistent `getCompletedCount` test and a wrong suite count, both fixed to cite the real `app.smoke.test.js:30-37` JOIN assertion + the Vitest glob). PR review: round-1 2-Opus panel FAIL (AC cited nonexistent test / unverifiable evidence — the gate working), fixed, round-2 fresh Opus PASS with CI confirmed green on the head SHA. Merge-on-green (maintenance, not visual/product-direction).
- #90 — adopt the mattpocock design skills (improve-codebase-architecture, codebase-design, grilling, domain-modeling): new root `CONTEXT.md` domain glossary grounded in the code (guest/task/submission/badge/points/leaderboard/gallery, `admin`=Task Master, plus a "Vision terms — not built yet" section for prize/slideshow/favorites/likes/comments/per-photo-points so no one greps for unimplemented features) that the skills read as their `CONTEXT.md`; the vendored skills are tracked under `.agents/skills/**` with `skills-lock.json` (source of truth), while `.claude/skills/` (a symlink mirror that would commit as broken text under `core.symlinks=false`) and generated `coverage/` are gitignored; README "Where things live" distinguishes the repo's own pipeline `skills/` from the installed `.agents/skills/`. `CONTEXT.md` also records the repo convention that ADRs live in `DESIGN.md`, not `docs/adr/`. Docs/config change (not system-level). Reviewed: issue PASS (reviewer-issue); PR panel unanimous PASS (two independent Opus reviewers); bias gate clean before fan-out. Merge-on-green (not visual/product-direction).
- f0c3cd1 — #47 — enforcement: bias-gate + severity-adjudicator as tree-bound evidence artifacts, fail-closed. New single-writers `tools/persist-bias-gate.ps1` (schema `bg1`) and `tools/persist-adjudication.ps1` (schema `adj1`), kept as separate scripts on purpose (self-attestation-surface isolation); `tools/verdict-core.ps1` gains `Test-BiasGateSatisfied` reusing the shared `Read-Evidence` (param renamed `$EvidenceRoot`) with per-artifact FAIL-wins; `tools/validate-verdict.ps1` requires a PASS bias-gate artifact bound to the exact tree whenever the auto-computed bar ≥ 2 (a system-level path is staged) and fails closed — while an explicit `-Required` override no longer consults ambient staging (fixed a cwd-coupling that broke `verdict-gate` AC5). The original #47's "N distinct artifact-backed verdicts, fail-closed" core already shipped in #53's commit gate, so this issue was reconciled down to the genuinely-missing bias-gate enforcement + the adjudicator record; the "did the step actually run" guarantee stays out of scope (needs the program-driven runner — the bar is tamper-evident, not tamper-proof). Dogfooded: this system-level commit had to satisfy its own new bias-gate check. Reviewed: issue PASS (reviewer-issue + reviewer-architecture); PR two independent Opus PASSes + design-philosophy PASS on the final tree over **4 rounds** — design-philosophy FAIL round 1 (a duplicated `Read-BiasGate` reader — the review having teeth), FAIL round 3 (parameter-naming minors), both fixed and re-reviewed; a bias gate ran and was clean before every review fan-out (it caught a real intent-leak in the first issue-review briefing). System-level change — left as an **open PR for the owner to merge** (orchestrator conflict of interest, per the issue). Follow-ups filed: adjudication artifact has no consumer (record-only by design); reviewer `file:line` citations were repeatedly out-of-range (review-quality nit); stale `see issue #46` reference in `tools/persist-review.ps1`; optional `-BiasGateRoot`-unset direct test.
- #42 — full-resolution photo view: new `GET /p/:submissionId` renders the original-resolution image + caption + task/uploader, with server-computed previous(newer)/next(older) links over the gallery's `created_at DESC, id DESC` total order (keyset tuple comparison), and `photo.js` maps ArrowLeft/Right + touch-swipe onto those server-rendered hrefs so navigation still works with JS off. Taken-down and nonexistent/non-numeric ids → 404, and taken-down photos are skipped in the prev/next chain (every query carries `taken_down = 0`). Gallery + public-profile thumbnails now link to `/p/<id>` (the old click-to-lightbox is retired). Likes/comments were requested on the original #42 but are OUT of scope per the confirmed north star (`likes / feeds` excluded) — a scope AC guards against a social layer, and expanding it would need a north-star change first (Goal D). Visual/product-direction — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel unanimous PASS; design-philosophy PASS. Follow-up filed: AC3 literal-scenario test, back-link class consistency, drop unused thumb_path column.
- #41 — dense photo-board gallery + organize by task/user/chronological: `GET /gallery` now takes `?view=recent|task|user` (recent = default, newest-first; unknown view falls back to recent) and an optional `?task=<id>` filter, with the task list/detail pages linking to `/gallery?task=<id>` so tapping a task opens its gallery (Goal C/D). Grid is gapless (`gap:0`, `.gallery-thumb` `aspect-ratio:1; object-fit:cover`) for a shoulder-to-shoulder photo board; grouped views render `<h2 class="gallery-group-heading">` per task/person. Query assembly is centralized in one `galleryQuery(kind, taskFilter, …)` helper (single source of the `taken_down=0` guard, AC6) and rendering in one `renderGallery()` helper; new `tests/gallery-views.test.js` (14 tests) covers ordering, view switching + fallback, task filter, and taken-down exclusion. Visual/product-direction — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel unanimous PASS; design-philosophy PASS after two fix rounds (query-assembly information-leakage hardened). Follow-ups filed: group "by person" by guest id (not name); drop duplicate `#galleryGrid` id in grouped views.
- #40 — alpha mobile-design pass: `.task-link` gets `min-width:0` so a long title/description can't push a task row past the mobile column (description already truncates; done/todo pill stays pinned); guest-home profile header collapses points + "Edit profile" onto one line with a mid-dot divider; new `src/utils/initials.js` (exposed as `app.locals.initials`) derives two-letter avatar initials ("Ava Fenwick"→"AF", "Cher"→"C", ""→"") and replaces four inline single-letter fallbacks across guest-home/public-profile/leaderboard; `crowdfav.svg` orbiting circles moved inward (radius 46→30). Serves Goals A/B (finished-feeling mobile UI). Visual — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel (3 Opus) unanimous PASS; design-philosophy PASS.

## 2026-06-30

- #44 — maintenance mode: a `MAINTENANCE` config flag makes guest routes serve an on-brand 503 "We'll be right back" page (reusing the error-page design) with a `Retry-After` header, while `/admin` stays reachable so the host can recover; plus `scripts/serve-resilient.js`, a no-dependency wrapper that relaunches the server on crash with backoff + a crash-loop cap and forwards SIGINT/SIGTERM to the child. Resilience for the live event (Goal A). Behavior decided from the goals (the issue left it open); the page reuses the established design system, not new product direction.
- #71 — make admin-login bcrypt async: `POST /admin/login` now uses `await bcrypt.compare` instead of the synchronous `bcrypt.compareSync`, so a burst of login attempts can't block the single event loop and freeze the app for guests (Goal A). Branch order unchanged, so #49's invariant (a correct password authenticates even during a lockout) is preserved; new `tests/login-lockout-async.test.js` proves it. Surfaced by the #58 security review.
- #58 — triage the 34 pre-existing high-severity CodeQL alerts: all non-exploitable at this app's threat model. path-injection + DOM-XSS = false positives (multer generates random filenames so the client's name never reaches an fs sink — now proven by `tests/upload-filename-safety.test.js`; `javascript:` on an `<img>.src` does not execute); missing-rate-limiting = dismiss-with-justification (admin-login lockout is DoS-safe, other routes auth-gated or cheap reads, a limiter adds a Cloudflare proxy-trust footgun). Reasoning recorded in `docs/security/codeql-triage-2026-06.md`; triaged as non-exploitable; verdicts recorded in docs/security/codeql-triage-2026-06.md and confirmed through adversarial security review before merge. Alerts dismissed in code scanning post-merge.
- #59 — regression-guard the destructive-action confirmations: new `tests/destructive-confirm.test.js` renders the admin guests/photos/tasks pages as admin and asserts each destructive form (guest delete, photo takedown, task delete), isolated by its delete/takedown action route, carries `data-confirm=` and `method="post"` — so a future template edit (e.g. #40) can't silently drop a confirmation.
- #61 — Dependabot integration: two-tier merge policy (`auto` on green CI / `review` held) via a testable classifier `tools/classify-dep-pr.ps1` (single source of truth for the wedding-critical dep list, drift-guarded against `CLAUDE.md` and `.github/dependabot.yml`); grouped `dependabot.yml` with wedding-critical deps excluded so held items arrive as individual PRs; policy documented in `CLAUDE.md`/`orchestrator.md`. Exit authorized by the severity adjudicator (remaining defects inconsequential). Triage of the 14 open PRs follows per AC8.
- #62 — issue-lifecycle review marker: issues are born `needs-issue-review`; the label is cleared only by a recorded issue-review PASS via the separate reader-gated `tools/clear-issue-marker.ps1` (evidence writer stays board-free, so separation-of-writers holds); adds board-wide `tools/audit-issue-markers.ps1`; born-marked creation + narrowed bookkeeping carve-out documented in the pipeline.

## 2026-06-28

Repo initialized from the as-built Garden Party Pastels app; an adversarial review and refactor are underway.
