# Build Log

Reverse-chronological record of notable changes to the repo.

## Entry conventions

Three entry types, appended in reverse-chronological order:

- `<sha> — #<n> — <summary>` — a committed issue; counted toward the periodic audit threshold.
- `[AUDIT] <sha> — <summary>` — full-system architectural audit, run on every 5th counted entry; excluded from the count.
- `[HALT] #<n> — <reason>` — segment halted at the impasse stop condition; the work is not committed.

The run-time Live-log ledger (per-increment `[HH:MM] elapsed=…` lines) lives in `docs/RESUME-STATE.md`.

## 2026-07-02

- 7a48c9c — #128 — infra(review): program-driven review runner. New `tools/review-runner.ps1` (+ `tools/review-verdict.schema.md`, `tests/review-runner.test.js`) is the mechanical front door for a reviewer panel: it reads each reviewer's verdict JSON from `-RunDir`, fail-closed citation-validates every cited `file:line` (`file-not-found`/`out-of-range` + a repo-root path-escape guard), refuses to run if `-TreeOid` ≠ the live staged tree, aggregates per `-Mode` (`both-pass` ≥2 all-PASS / `unanimous` ≥1 all-PASS — the protocol's two reviewer-count bars), and only on a fully clean pass calls the existing `persist-review.ps1` + `review_verdict.ps1` — so a recorded PASS is mechanical and a fabricated citation can't produce one. Reuses the single-writers (no new evidence surface); `DESIGN.md` documents it and the directory-of-JSON design choice. Bounded first version (consumes fixture verdict JSON; reviewer agents _emitting_ it is a later slice). System-level: issue-review PASS (reviewer-issue + reviewer-architecture); bias-gate clean before each fan-out; PR review two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the final tree over 6 rounds — the panel found and drove fixes for real defects: identical `-Mode` branches (information-leakage) → genuine divergence; `powershell` hardcoded → edition-detected `pwsh` for Linux CI; a `line: 0` fail-open (PowerShell `0 -ne ''` coercion) → string-compared guard; a path-escape prefix hole admitting `<root>-evil` siblings → separator-bounded check; nested-`pwsh` spawns timing out Linux CI → a 30s per-test timeout; plus `findings_count`/schema-doc nits. 14 isolated tests green (full suite 160 with #68's retry-selfheal). Rebased onto #68 so the pwsh-spawn-heavy suite has stable CI. Unblocks #94. Non-blocking follow-up (design-debt): the FAIL-wins/count aggregation is re-derived here rather than reusing `verdict-core`'s `Reduce-Verdicts`, and a comment overstates that reuse. Merge-on-green.
- f7f3f9b — #68 — test-infra: add vitest `test.retry: 2` to absorb transient pwsh-spawn flakes. On CI, `pwsh` cold-start contention can push a launcher-spawning test past the default 5s timeout — observed 2026-07-02 as identical commits going red/green by scheduling luck (`enforcement-bias-gate` AC2 timing out), the flake that was blocking the correct, reviewed #128 PR. A bounded global retry self-heals a transient miss while a genuine regression (fails every attempt) still fails. New `tests/retry-selfheal.test.js` proves it isn't inert (module-scoped counter: fails attempt 1, passes on retry; reds the suite under `retry: 0`). Graduated from backlog (issue-review PASS); PR routine panel unanimous — two `reviewer-pr` + `reviewer-design-philosophy`, nits only. Unblocks reliable green CI for the pwsh-spawn-heavy suite. Merge-on-green.
- cbddd1e — #117 — process(review): reviewers are read-only, no state-mutating git. Added an explicit `## Read-only` clause + the forbidden set (`git add`/`reset`/`restore`/`checkout`/`stash`/`commit`/`rm` + no file edits) to all ten reviewing agent specs (nine `reviewer-*.md` + `severity-adjudicator.md`); a `## Reviewers are read-only` rule to `standards/adversarial-review-protocol.md` carrying the 2026-07-02 `git restore` incident as rationale; and `## No mutation authority` (AC3) + `## Re-verify the tree oid before recording a verdict` (AC4) directives to `skills/spawn-adversarial-review.md`. The prose binds a reviewer even when instantiated with a broader tool set, closing the hole where a reviewer mutates the exact staged tree its verdict is bound to; `reviewer-tracker-sync` additionally forbids board mutation (`gh issue close`/`edit`) given its Bash/gh access. System-level change: issue-review PASS (reviewer-issue + reviewer-architecture, recorded earlier); the bias gate ran twice before fan-out and caught a real intent-leak — my briefings recited the forbidden-command list, which is the diff's own AC payload — fixed both times, then clean; PR review two independent `reviewer-pr` PASS + `reviewer-design-philosophy` PASS on the exact staged tree, single round, 0 defects (one nit). Follow-up nit (no action for this issue): the ~60-word read-only block is duplicated across 11–12 files — per-file prose is the intended design, but a drift-guard test (cf. `tests/classify-dep-pr.test.js`) would catch silent divergence. Correctness/process fix — merge-on-green.

## 2026-07-01

- #66 — bump dev-deps group (eslint 9→10, @eslint/js 9→10, eslint-config-prettier 9→10, prettier 3.9.1→3.9.4): auto-tier dev bump that arrived CI-red because eslint 10 added `no-useless-assignment` to `eslint:recommended`, flagging three dead-store initializers. Fixed first in #110, then #66 rebased onto that → `lint` green → merged. Dev-only; app unaffected (test 145/145).
- #109 (PR #110) — fix(lint): drop three dead-store initializers (`let ok`/`social`/`socialText` → bare `let`) in `src/routes/community.js:67`, `src/routes/guest.js:347`, `src/services/export.js:143`, flagged by eslint 10 `no-useless-assignment`. Behavior-preserving — every `try`/`catch` branch assigns before first read (traced by both reviewers). Issue-review PASS; PR 2-Opus panel PASS + design-philosophy PASS; committed through the local commit gate (3 recorded reviewer verdicts + issue-review). Unblocks #66.
- #97 — bump multer 1.4.5-lts.1 → 2.2.0 (Dependabot PR #11, wedding-critical, `review` tier, security-maintained line): only 2.0 breaking change is the Node floor (repo is Node ≥20). Both upload paths audited; size-limit rejection (`LIMIT_FILE_SIZE`→302) and disk-storage filename shape verified. Issue-review PASS (4 rounds — AC accuracy tightened); PR round-1 2-Opus panel FAIL (an AC over-claimed an existing size-limit test — the gate working), reframed to CI-observed + source-trace, round-2 fresh Opus PASS. No source change. Merge-on-green.
- #98 — bump bcryptjs 2.4.3 → 3.0.3 (Dependabot PR #13, wedding-critical, auth, `review` tier): the dominant risk — an old `$2a$` hash failing to verify — was disproven by regenerating a real 2.4.3 hash and confirming `await bcrypt.compare` returns `true` under 3.0.3 on the live login path (and `false` for a wrong password). 3.x default `$2b$` prefix already tolerated by the pw-strength test. Issue-review PASS (2 rounds — first draft cited sync `compareSync`; ground truth is async `compare` at `auth.js:180`); PR review unanimous 3-of-3 Opus (high-stakes: auth). No source change. Merge-on-green.
- #101 — bump ejs 3.1.10 → 6.0.1 (Dependabot PR #9, wedding-critical, all views, `review` tier): the largest jump (3 majors). ejs 6's CJS `require('ejs')`/`__express`/`include()` surface is unchanged (v5 removed only the unused `client` option; v6 changed only the ESM build). Verified beyond CI (archiver lesson): all 21 templates + 13 live routes rendered under 6.0.1, nested `include()` resolves. Issue-review PASS; PR round-1 split (1 PASS, 1 FAIL on transient pending CI), round-2 fresh Opus PASS on settled green CI. No template edit needed. Merge-on-green.
- #103 (PR #67) — express 4.21.2 → 5.2.1: **DEFERRED, not merged.** A whole-framework major (path-to-regexp route syntax, request/response method changes) with real route-breakage risk and zero guest-facing benefit; Express 4.x is still maintained, so no forcing function before the Aug 7 event. PR closed; migration tracked in backlog #103 (graduate 2026-08-08, gated on a per-route verification pass). Owner-boundary risk call, decided from the North Star.
- #65 — bump the actions-all group with 4 updates (Dependabot PR #65, `auto` tier): `actions/checkout` v4→v7, `actions/setup-node` v4→v6, `actions/upload-artifact` v4→v7, `github/codeql-action` v3→v4 across `ci.yml`/`codeql.yml`. CI-tooling only, cannot affect the running app; CI green, MERGEABLE. Merged on green per the auto-tier policy — no tracked decision required.
- 82c2d76 — #99 — bump sharp 0.33.5 → 0.35.2 (Dependabot PR #14, wedding-critical, `review` tier): thumbnail/avatar image processing, no source change. sharp 0.34/0.35 breaking changes (Node floor →20.9, libvips 8.18.3, install-script model, `failOnError`/`jp2k` removals) don't touch the methods we use (`sharp()`/`.rotate()`/`.resize()`/`.jpeg()`/`.toFile()`). Verified in a worktree: prebuilt `@img/sharp-win32-x64` binary installs with no build tools, `require('sharp')` loads, full suite green, and `makeThumb` yields a 400px JPEG from an 800px source + no upscaling from a 200px source. Issue-review PASS; PR review 2-Opus panel unanimous PASS. Merge-on-green. Follow-up nit: stale `sharp@0.33.5` comment at `photos.js:35`.
- #47/#61 batch note — Dependabot triage: the June-28 batch (#6/#9/#10/#11/#13/#14) was all ~56 commits behind `main`; each is updated onto current `main` so CI exercises the real tree before merge. Verification is behavioral, not CI-trusting: it caught that **archiver 8.0.0 (PR #6) is a breaking pure-ESM rewrite** removing the callable `archiver('zip',…)` factory that `export.js:240` depends on — CI was green only because no test exercises `streamExportZip`. #6 closed (kept archiver 7); migration + the missing export-zip test tracked in #102 (graduate 2026-08-08).
- 9808836 — #96 — bump better-sqlite3 12.2.0 → 12.11.1 (Dependabot PR #10, wedding-critical, `review` tier): same-major minor bump, no source change. The whole June-28 Dependabot batch was ~56 commits behind `main`, so their original green CI tested a 1-suite tree — branch updated onto current `main` and CI re-run green on the full Vitest suite before merge. API audit: only the stable core surface (`new Database`/`.pragma`/`.exec`/`.prepare().get/.run/.all`/`.transaction`) is used, unchanged across v12.3.0..v12.11.1. Issue-review PASS (2 rounds — a first amendment cited a nonexistent `getCompletedCount` test and a wrong suite count, both fixed to cite the real `app.smoke.test.js:30-37` JOIN assertion + the Vitest glob). PR review: round-1 2-Opus panel FAIL (AC cited nonexistent test / unverifiable evidence — the gate working), fixed, round-2 fresh Opus PASS with CI confirmed green on the head SHA. Merge-on-green (maintenance, not visual/product-direction).
- #90 — adopt the mattpocock design skills (improve-codebase-architecture, codebase-design, grilling, domain-modeling): new root `CONTEXT.md` domain glossary grounded in the code (guest/task/submission/badge/points/leaderboard/gallery, `admin`=Task Master, plus a "Vision terms — not built yet" section for prize/slideshow/favorites/likes/comments/per-photo-points so no one greps for unimplemented features) that the skills read as their `CONTEXT.md`; the vendored skills are tracked under `.agents/skills/**` with `skills-lock.json` (source of truth), while `.claude/skills/` (a symlink mirror that would commit as broken text under `core.symlinks=false`) and generated `coverage/` are gitignored; README "Where things live" distinguishes the repo's own pipeline `skills/` from the installed `.agents/skills/`. `CONTEXT.md` also records the repo convention that ADRs live in `DESIGN.md`, not `docs/adr/`. Docs/config change (not system-level). Reviewed: issue PASS (reviewer-issue); PR panel unanimous PASS (two independent Opus reviewers); bias gate clean before fan-out. Merge-on-green (not visual/product-direction).
- f0c3cd1 — #47 — enforcement: bias-gate + severity-adjudicator as tree-bound evidence artifacts, fail-closed. New single-writers `tools/persist-bias-gate.ps1` (schema `bg1`) and `tools/persist-adjudication.ps1` (schema `adj1`), kept as separate scripts on purpose (self-attestation-surface isolation); `tools/verdict-core.ps1` gains `Test-BiasGateSatisfied` reusing the shared `Read-Evidence` (param renamed `$EvidenceRoot`) with per-artifact FAIL-wins; `tools/validate-verdict.ps1` requires a PASS bias-gate artifact bound to the exact tree whenever the auto-computed bar ≥ 2 (a system-level path is staged) and fails closed — while an explicit `-Required` override no longer consults ambient staging (fixed a cwd-coupling that broke `verdict-gate` AC5). The original #47's "N distinct artifact-backed verdicts, fail-closed" core already shipped in #53's commit gate, so this issue was reconciled down to the genuinely-missing bias-gate enforcement + the adjudicator record; the "did the step actually run" guarantee stays out of scope (needs the program-driven runner — the bar is tamper-evident, not tamper-proof). Dogfooded: this system-level commit had to satisfy its own new bias-gate check. Reviewed: issue PASS (reviewer-issue + reviewer-architecture); PR two independent Opus PASSes + design-philosophy PASS on the final tree over **4 rounds** — design-philosophy FAIL round 1 (a duplicated `Read-BiasGate` reader — the review having teeth), FAIL round 3 (parameter-naming minors), both fixed and re-reviewed; a bias gate ran and was clean before every review fan-out (it caught a real intent-leak in the first issue-review briefing). System-level change — left as an **open PR for the owner to merge** (orchestrator conflict of interest, per the issue). Follow-ups filed: adjudication artifact has no consumer (record-only by design); reviewer `file:line` citations were repeatedly out-of-range (review-quality nit); stale `see issue #46` reference in `tools/persist-review.ps1`; optional `-BiasGateRoot`-unset direct test.
- #42 — full-resolution photo view: new `GET /p/:submissionId` renders the original-resolution image + caption + task/uploader, with server-computed previous(newer)/next(older) links over the gallery's `created_at DESC, id DESC` total order (keyset tuple comparison), and `photo.js` maps ArrowLeft/Right + touch-swipe onto those server-rendered hrefs so navigation still works with JS off. Taken-down and nonexistent/non-numeric ids → 404, and taken-down photos are skipped in the prev/next chain (every query carries `taken_down = 0`). Gallery + public-profile thumbnails now link to `/p/<id>` (the old click-to-lightbox is retired). Likes/comments were requested on the original #42 but are OUT of scope per the confirmed north star (`likes / feeds` excluded) — a scope AC guards against a social layer, and expanding it would need a north-star change first (Goal D). Visual/product-direction — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel unanimous PASS; design-philosophy PASS. Follow-up filed: AC3 literal-scenario test, back-link class consistency, drop unused thumb_path column.
- #41 — dense photo-board gallery + organize by task/user/chronological: `GET /gallery` now takes `?view=recent|task|user` (recent = default, newest-first; unknown view falls back to recent) and an optional `?task=<id>` filter, with the task list/detail pages linking to `/gallery?task=<id>` so tapping a task opens its gallery (Goal C/D). Grid is gapless (`gap:0`, `.gallery-thumb` `aspect-ratio:1; object-fit:cover`) for a shoulder-to-shoulder photo board; grouped views render `<h2 class="gallery-group-heading">` per task/person. Query assembly is centralized in one `galleryQuery(kind, taskFilter, …)` helper (single source of the `taken_down=0` guard, AC6) and rendering in one `renderGallery()` helper; new `tests/gallery-views.test.js` (14 tests) covers ordering, view switching + fallback, task filter, and taken-down exclusion. Visual/product-direction — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel unanimous PASS; design-philosophy PASS after two fix rounds (query-assembly information-leakage hardened). Follow-ups filed: group "by person" by guest id (not name); drop duplicate `#galleryGrid` id in grouped views.
- #40 — alpha mobile-design pass: `.task-link` gets `min-width:0` so a long title/description can't push a task row past the mobile column (description already truncates; done/todo pill stays pinned); guest-home profile header collapses points + "Edit profile" onto one line with a mid-dot divider; new `src/utils/initials.js` (exposed as `app.locals.initials`) derives two-letter avatar initials ("Ava Fenwick"→"AF", "Cher"→"C", ""→"") and replaces four inline single-letter fallbacks across guest-home/public-profile/leaderboard; `crowdfav.svg` orbiting circles moved inward (radius 46→30). Serves Goals A/B (finished-feeling mobile UI). Visual — left as an open PR for the owner's eye. Reviewed: issue PASS; PR panel (3 Opus) unanimous PASS; design-philosophy PASS.

## 2026-06-30

- #44 — maintenance mode: a `MAINTENANCE` config flag makes guest routes serve an on-brand 503 "We'll be right back" page (reusing the error-page design) with a `Retry-After` header, while `/admin` stays reachable so the host can recover; plus `scripts/serve-resilient.js`, a no-dependency wrapper that relaunches the server on crash with backoff + a crash-loop cap and forwards SIGINT/SIGTERM to the child. Resilience for the live event (Goal A). Behavior decided from the goals (the issue left it open); the page reuses the established design system, not new product direction.
- #71 — make admin-login bcrypt async: `POST /admin/login` now uses `await bcrypt.compare` instead of the synchronous `bcrypt.compareSync`, so a burst of login attempts can't block the single event loop and freeze the app for guests (Goal A). Branch order unchanged, so #49's invariant (a correct password authenticates even during a lockout) is preserved; new `tests/login-lockout-async.test.js` proves it. Surfaced by the #58 security review.
- #58 — triage the 34 pre-existing high-severity CodeQL alerts: all non-exploitable at this app's threat model. path-injection + DOM-XSS = false positives (multer generates random filenames so the client's name never reaches an fs sink — now proven by `tests/upload-filename-safety.test.js`; `javascript:` on an `<img>.src` does not execute); missing-rate-limiting = dismiss-with-justification (admin-login lockout is DoS-safe, other routes auth-gated or cheap reads, a limiter adds a Cloudflare proxy-trust footgun). Reasoning recorded in `docs/security/codeql-triage-2026-06.md`; triaged as non-exploitable; verdicts recorded in docs/security/codeql-triage-2026-06.md and confirmed through adversarial security review before merge. Alerts dismissed in code scanning post-merge.
- #59 — regression-guard the destructive-action confirmations: new `tests/destructive-confirm.test.js` renders the admin guests/photos/tasks pages as admin and asserts each destructive form (guest delete, photo takedown, task delete), isolated by its delete/takedown action route, carries `data-confirm=` and `method="post"` — so a future template edit (e.g. #40) can't silently drop a confirmation.
- #61 — Dependabot integration: two-tier merge policy (`auto` on green CI / `review` held) via a testable classifier `tools/classify-dep-pr.ps1` (single source of truth for the wedding-critical dep list, drift-guarded against `CLAUDE.md` and `.github/dependabot.yml`); grouped `dependabot.yml` with wedding-critical deps excluded so held items arrive as individual PRs; policy documented in `CLAUDE.md`/`orchestrator.md`. Exit authorized by the severity adjudicator (remaining defects inconsequential). Triage of the 14 open PRs follows per AC8.
- #62 — issue-lifecycle review marker: issues are born `needs-issue-review`; the label is cleared only by a recorded issue-review PASS via the separate reader-gated `tools/clear-issue-marker.ps1` (evidence writer stays board-free, so separation-of-writers holds); adds board-wide `tools/audit-issue-markers.ps1`; born-marked creation + narrowed bookkeeping carve-out documented in the pipeline.

## 2026-06-28

Repo initialized from the as-built Garden Party Pastels app; an adversarial review and refactor are underway.
