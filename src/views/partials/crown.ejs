<%# src/views/partials/crown.ejs — champion mark above the 1st-place block. %>
<%# Accepts optional local `size` (default 30). Fill via CSS color. %>
<svg viewBox="0 0 32 24" width="<%= (typeof size !== 'undefined' && size) ? size : 30 %>" height="<%= ((typeof size !== 'undefined' && size) ? size : 30) * 24 / 32 %>" aria-hidden="true" style="fill:currentColor">
  <path d="M3 8.5 L8 13.5 L16 3.5 L24 13.5 L29 8.5 L27 20.5 L5 20.5 Z"/>
  <circle cx="3" cy="8.5" r="2.4"/>
  <circle cx="16" cy="3.5" r="2.6"/>
  <circle cx="29" cy="8.5" r="2.4"/>
</svg>
