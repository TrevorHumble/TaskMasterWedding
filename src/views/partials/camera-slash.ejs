<%# src/views/partials/camera-slash.ejs
    Issue #1042 review fix (MAJOR 1): the camera-with-a-slash outline glyph,
    now a single shared partial instead of four hand-copied inline SVGs that
    had already diverged in stroke width and size. Every call site, the
    day-of band (partials/header.ejs), the how-to-play onboarding row, the
    no-photos hero, and the recap row's glyph (services/notifications.js, via
    ejs.compile), renders THIS drawing, with only `size`/`strokeWidth`
    varying. %>
<%
  var _size = (typeof size !== 'undefined' && size) ? size : 22;
  var _sw = (typeof strokeWidth !== 'undefined' && strokeWidth) ? strokeWidth : 1.7;
-%>
<svg viewBox="0 0 24 24" width="<%= _size %>" height="<%= _size %>" fill="none" stroke="currentColor" stroke-width="<%= _sw %>" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M3 8.5A1.5 1.5 0 0 1 4.5 7h2.6l1.2-1.8h6.4L15.9 7h3.6A1.5 1.5 0 0 1 21 8.5v9a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 3 17.5v-9Z"/><circle cx="12" cy="13" r="3.4"/><path d="m3.6 3.6 16.8 16.8" stroke-linecap="round"/></svg>
