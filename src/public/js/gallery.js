// src/public/js/gallery.js
// The lightbox was removed when thumbnails were rewired to link directly
// to /p/<id> (issue #42). This file is kept as an empty module so any
// pageScript reference that wasn't cleaned up does not cause a 404.
'use strict';
